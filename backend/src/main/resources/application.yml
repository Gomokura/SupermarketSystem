server:
  port: 8080

spring:
  application:
    name: supermarket-backend

  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    # 11g 常用 SID 连接；若仍连不上可用 lsnrctl status 看「服务名」再改回 //host:port/服务名
    url: jdbc:oracle:thin:@localhost:1521:orcl
    username: system
    password: Liu12345
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 600000
      connection-test-query: SELECT 1 FROM DUAL

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    default-property-inclusion: non_null

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.supermarket.entity
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    call-setters-on-nulls: true

jwt:
  # HS256 要求 secret >= 32 字节
  secret: supermarket-system-secret-key-2024-oracle-xe
  expiration: 86400000  # 24小时（毫秒）

logging:
  level:
    com.supermarket: debug
    com.baomidou: info
