# 超市管理系统设计文档

## 一、数据库设计（30分）

### 1.1 数据库表结构

#### users（用户表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| user_id | NUMBER | 用户ID | 主键 |
| username | VARCHAR2(50) | 用户名 | 唯一，非空 |
| password | VARCHAR2(100) | 密码(MD5) | 非空 |
| real_name | VARCHAR2(50) | 真实姓名 | |
| role | VARCHAR2(20) | 角色 | admin/user |
| phone | VARCHAR2(20) | 电话 | |
| status | VARCHAR2(10) | 状态 | 正常/冻结 |
| create_time | DATE | 创建时间 | 默认当前时间 |

#### categories（商品分类表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| category_id | NUMBER | 分类ID | 主键 |
| category_name | VARCHAR2(50) | 分类名称 | 非空 |
| description | VARCHAR2(200) | 描述 | |

#### products（商品表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| product_id | NUMBER | 商品ID | 主键 |
| product_name | VARCHAR2(100) | 商品名称 | 非空 |
| category_id | NUMBER | 分类ID | 外键 |
| price | NUMBER(10,2) | 价格 | 非空 |
| stock | NUMBER | 库存 | 默认0 |
| unit | VARCHAR2(20) | 单位 | |
| supplier | VARCHAR2(100) | 供应商 | |
| status | VARCHAR2(10) | 状态 | 上架/下架 |
| create_time | DATE | 创建时间 | 默认当前时间 |

#### orders（订单表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| order_id | NUMBER | 订单ID | 主键 |
| user_id | NUMBER | 用户ID | 外键 |
| total_amount | NUMBER(10,2) | 总金额 | |
| order_status | VARCHAR2(20) | 订单状态 | 待支付/待发货/配送中/已完成/已取消 |
| payment_method | VARCHAR2(20) | 支付方式 | |
| order_time | DATE | 下单时间 | 默认当前时间 |

#### order_items（订单明细表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| order_item_id | NUMBER | 明细ID | 主键 |
| order_id | NUMBER | 订单ID | 外键 |
| product_id | NUMBER | 商品ID | 外键 |
| product_name | VARCHAR2(100) | 商品名称 | |
| quantity | NUMBER | 数量 | 非空 |
| price | NUMBER(10,2) | 单价 | |
| subtotal | NUMBER(10,2) | 小计 | |

#### cart（购物车表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| cart_id | NUMBER | 购物车ID | 主键 |
| user_id | NUMBER | 用户ID | 外键 |
| product_id | NUMBER | 商品ID | 外键 |
| quantity | NUMBER | 数量 | |

#### address（收货地址表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| address_id | NUMBER | 地址ID | 主键 |
| user_id | NUMBER | 用户ID | 外键 |
| receiver | VARCHAR2(50) | 收货人 | |
| phone | VARCHAR2(20) | 手机号 | |
| detail | VARCHAR2(200) | 详细地址 | |
| is_default | NUMBER | 是否默认 | 0/1 |

### 1.2 索引设计

| 索引名 | 字段 | 用途 |
|--------|------|------|
| idx_product_name | product_name | 商品名称搜索 |
| idx_product_category | category_id | 分类查询 |
| idx_order_user | user_id | 用户订单查询 |
| idx_order_time | order_time | 时间范围查询 |

---

## 二、系统架构设计

### 2.1 前后端分离架构

```
┌─────────────┐      REST API       ┌─────────────┐
│  Vue 3 前端  │ ←─────────────────→ │ Spring Boot │
│  (端口3000)  │      JSON          │  (端口8080) │
└─────────────┘                     └─────────────┘
                                            │
                                            ↓
                                     ┌─────────────┐
                                     │   Oracle    │
                                     │  (端口1521) │
                                     └─────────────┘
```

### 2.2 后端分层架构

| 层级 | 职责 | 组件 |
|------|------|------|
| Controller | 接收请求，返回响应 | REST API |
| Service | 业务逻辑处理 | AuthService, ProductService等 |
| Mapper | 数据访问 | MyBatis Plus |
| Entity | 数据模型 | User, Product等 |

### 2.3 前端架构

| 模块 | 技术 | 职责 |
|------|------|------|
| 路由 | Vue Router | 页面导航 |
| 状态 | Pinia | 用户状态管理 |
| HTTP | Axios | API请求 |
| UI | Element Plus | 组件库 |

---

## 三、API设计

### 3.1 认证接口

```
POST /api/auth/login
请求体: { "username": "admin", "password": "admin123" }
响应: { "code": 200, "data": "jwt_token..." }

POST /api/auth/register
请求体: { "username": "...", "password": "...", "realName": "...", "phone": "..." }
响应: { "code": 200, "message": "success" }
```

### 3.2 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {...}
}
```

---

## 四、功能设计

### 4.1 JWT认证流程

```
1. 用户登录 → POST /api/auth/login
2. 后端验证 → 返回 JWT Token
3. 前端存储 → localStorage.setItem('token', token)
4. 请求携带 → Authorization: Bearer <token>
5. 拦截器验证 → 验证Token有效性
6. 请求放行 → 执行业务逻辑
```

### 4.2 购物车流程

```
1. 添加商品 → POST /api/cart/add
2. 购物车列表 → GET /api/cart/list
3. 修改数量 → PUT /api/cart/update
4. 删除商品 → DELETE /api/cart/{cartId}
5. 清空购物车 → DELETE /api/cart/clear
```

### 4.3 订单流程（事务）

```
1. 创建订单 → POST /api/orders/create
   ├── 验证地址
   ├── 验证商品库存
   ├── 创建订单记录
   ├── 创建订单明细
   ├── 扣减库存
   ├── 记录库存日志
   ├── 清空购物车
   └── 生成支付记录（可选）
```

---

## 五、界面设计

### 5.1 用户端页面

| 页面 | 路由 | 功能 |
|------|------|------|
| 登录 | /login | 用户登录、注册 |
| 首页 | /home | 商品推荐 |
| 商品 | /products | 商品列表、搜索 |
| 购物车 | /cart | 购物车管理 |
| 结算 | /checkout | 订单确认、支付 |
| 订单 | /orders | 订单列表 |
| 订单详情 | /orders/:id | 订单明细 |
| 地址 | /address | 收货地址管理 |
| 个人中心 | /profile | 个人信息 |

### 5.2 管理端页面

| 页面 | 路由 | 功能 |
|------|------|------|
| Dashboard | /admin/dashboard | 数据统计 |
| 商品管理 | /admin/products | 商品CRUD |
| 分类管理 | /admin/categories | 分类CRUD |
| 用户管理 | /admin/users | 用户管理 |
| 订单管理 | /admin/orders | 订单管理 |
| 库存管理 | /admin/inventory | 入库/出库 |
| 配送管理 | /admin/deliveries | 配送状态 |
| 促销管理 | /admin/promotions | 促销活动 |
| 财务报表 | /admin/finance | 营收统计 |

---

## 六、安全设计

### 6.1 JWT安全

- Token有效期：24小时
- 签名算法：HMAC-SHA256
- 存储方式：客户端 localStorage

### 6.2 SQL注入防护

- 使用 MyBatis Plus 参数化查询
- 不拼接用户输入到 SQL

### 6.3 CORS跨域

- 允许前端开发服务器访问
- 支持凭证（Cookie/Token）
