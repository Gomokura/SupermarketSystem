# 前端开发分工文档

> 基于现有后端 API 完成前端页面开发，技术栈：Vue 3 + Element Plus + Pinia + Vue Router + Axios + ECharts
> 覆盖六端：顾客端 / 管理后台 / 收银端 / 仓储进货端 / 数据看板端 / 配送员端

---

## 分工总览

| 成员 | 负责模块 | 新建页面数 | 改造页面数 | 工作量 |
|------|---------|-----------|-----------|--------|
| 组长 A | 基础架构 + 顾客端核心页面完善 | 2 | 3 + 路由 | ⭐⭐⭐ |
| 成员 B | 顾客端订单/售后/评价 + B端售后/评价管理 | 4 | 3 | ⭐⭐⭐⭐⭐ |
| 成员 C | 顾客端会员中心 + 供应商/审计日志 | 5 | 2 | ⭐⭐⭐⭐⭐ |
| 成员 D | 管理后台完善 + 仓储进货端采购管理 + 优惠券管理 | 6 | 3 | ⭐⭐⭐⭐⭐ |
| 成员 E | 收银端 + 配送员端 + 数据看板端 + 仓储盘点/报损 + 配送员管理 | 7 | 1 | ⭐⭐⭐⭐⭐ |

> 仓储进货端通过 `/admin` 路由以仓管角色访问，数据看板端通过 `/admin` Dashboard 访问，无需独立路由前缀。

---

## 组长 A — 基础架构 + 顾客端核心页面完善

### 职责说明
负责项目整体前端架构维护，完善顾客端最核心的入口页面，并统一注册所有成员新建页面的路由。

### 具体任务

#### 1. `frontend/src/views/user/Home.vue` — 完善首页
- Banner 轮播图：调用 `GET /banners/list`，使用 `el-carousel` 展示
- 「热销商品」区块：调用 `GET /products/list?sortBy=sales&sortOrder=desc&pageSize=8`
- 「新品上市」区块：调用 `GET /products/list?sortBy=createTime&sortOrder=desc&pageSize=8`
- 「促销活动入口」卡片：调用 `GET /seckill/activities?state=running` 展示进行中的秒杀活动，点击跳转 `/seckill`
- 修复推荐商品区块，改为调用 `GET /products/recommended`

#### 2. `frontend/src/views/user/Products.vue` — 完善商品列表页
- 添加价格区间筛选（最低价 / 最高价输入框）
- 添加排序方式切换（综合 / 价格升降 / 销量 / 评分）
- 添加品牌筛选下拉框：调用 `GET /brands/list`
- 商品卡片点击跳转到 `/products/:id`

#### 3. 新建 `frontend/src/views/user/ProductDetail.vue` — 商品详情页
- 路由：`/products/:id`
- 展示：商品图片、名称、价格/原价、库存、描述
- SKU 规格选择：调用 `GET /products/{productId}/skus`，价格随规格切换
- 低库存提示（库存 < 预警阈值时显示「仅剩 XX 件」）
- 加入购物车：调用 `POST /cart/add`
- 商品评价列表：调用 `GET /reviews/product/{productId}`
- 同分类相关商品推荐（底部）

#### 4. 新建 `frontend/src/views/user/Seckill.vue` — 秒杀活动页（顾客端）
- 路由：`/seckill`
- 活动列表：调用 `GET /seckill/activities`，支持 state 筛选（running/pending/ended）
- 点击活动展开详情，展示该活动下秒杀商品：调用 `GET /seckill/activities/{seckillId}/products`
- 商品卡片展示秒杀价、原价、剩余库存进度条
- 「立即抢购」按钮：调用 `POST /cart/add` 加入购物车后跳转结算

#### 5. `frontend/src/views/user/Checkout.vue` — 完善结算页
- 优惠券选择下拉：调用 `GET /coupons/available?orderAmount=xxx`
- 积分抵扣选项（勾选后自动计算最多抵扣 30%）
- 期望配送时间选择（今日/明日，早中晚时段）
- 备注输入框（传入 `remark` 字段）
- 价格明细：商品总价、优惠券减免、积分抵扣、实付金额

#### 6. `frontend/src/router/index.js` — 路由统一注册
所有成员新建页面的路由由组长统一注册：
```
/products/:id          → user/ProductDetail.vue
/seckill               → user/Seckill.vue
/favorites             → user/Favorites.vue
/after-sale            → user/AfterSale.vue
/review/:orderId       → user/Review.vue
/coupons               → user/Coupons.vue
/points-logs           → user/PointsLog.vue
/messages              → user/Messages.vue
/cashier               → cashier/Cashier.vue
/courier               → courier/Tasks.vue
/admin/admins          → admin/Admins.vue
/admin/brands          → admin/Brands.vue
/admin/banners         → admin/Banners.vue
/admin/coupons-manage  → admin/CouponsManage.vue
/admin/after-sales     → admin/AfterSales.vue
/admin/reviews         → admin/Reviews.vue
/admin/seckill         → admin/Seckill.vue
/admin/stocktake       → admin/Stocktake.vue
/admin/damage-records  → admin/DamageRecords.vue
/admin/purchase-orders → admin/PurchaseOrders.vue
/admin/suppliers       → admin/Suppliers.vue
/admin/audit-log       → admin/AuditLog.vue
/admin/couriers        → admin/Couriers.vue
```

---

## 成员 B — 顾客端订单/售后/评价 + B端售后/评价管理

### 职责说明
负责顾客端订单全流程、售后、评价页面，以及管理后台对应的售后审核和评价管理页面（同一业务域的两侧）。

### 具体任务

#### 1. `frontend/src/views/user/Orders.vue` — 完善订单列表
- 状态 Tab 栏：全部 / 待付款 / 待发货 / 配送中 / 待收货 / 已完成 / 已取消
- 点击 Tab 传入 `status` 参数调用 `GET /orders/list?status=xxx`
- 每条订单显示商品缩略图、订单号、金额、状态标签

#### 2. `frontend/src/views/user/OrderDetail.vue` — 完善订单详情
- 展示完整订单信息：商品明细、收货地址、订单号、下单时间
- 「去支付」按钮（待付款状态）：调用 `POST /orders/{orderId}/pay`
- 「取消订单」按钮（待付款/待发货状态）：调用 `PUT /orders/{orderId}/cancel`
- 配送进度展示：配送员姓名（脱敏电话）和配送状态（已取件/配送中/已送达）
- 「确认收货」按钮（待收货状态）：调用 `PUT /orders/{orderId}/confirm`
- 「再次购买」按钮（已完成状态）：调用 `POST /orders/{orderId}/reorder`
- 「申请售后」按钮（已完成状态）：跳转 `/after-sale?orderId=xxx`
- 「去评价」按钮（已完成且未评价时）：跳转 `/review/:orderId`

#### 3. `frontend/src/views/user/Cart.vue` — 完善购物车
- 每个商品前复选框（`el-checkbox`），支持全选/全不选
- 底部价格和件数只统计已勾选商品
- 已下架或缺货商品灰显，禁用勾选和数量修改
- 「去结算」只传已勾选商品 ID 给结算页

#### 3.5. `frontend/src/views/user/Favorites.vue` — 商品收藏（顾客端）
- 路由：`/favorites`
- 收藏列表：调用 `GET /favorites/my`，展示商品图片、名称、价格
- 取消收藏：调用 `DELETE /favorites/{productId}`
- 在 `ProductDetail.vue` 中增加「收藏/取消收藏」按钮：调用 `POST /favorites/{productId}` / `DELETE /favorites/{productId}`

#### 4. 新建 `frontend/src/views/user/AfterSale.vue` — 售后申请与进度（顾客端）
- 路由：`/after-sale`
- Tab：「申请售后」和「我的售后」
- 申请表单：售后类型（仅退款/退货退款）、原因文本域、上传凭证图片（`el-upload` 本地预览）
- 提交：调用 `POST /after-sales`
- 我的售后列表：调用 `GET /after-sales/my`，展示状态（待处理/已同意/已拒绝）
- 售后详情：调用 `GET /after-sales/{afterSaleId}`

#### 5. 新建 `frontend/src/views/user/Review.vue` — 提交评价（顾客端）
- 路由：`/review/:orderId`
- 展示该订单的商品列表
- 每个商品：1~5 星评分（`el-rate`）、文字评价、标签快选（好质量/包装好/物流快等）、匿名开关
- 提交：调用 `POST /reviews`

#### 6. 新建 `frontend/src/views/admin/AfterSales.vue` — 售后退款管理（管理后台）
- 路由：`/admin/after-sales`
- Tab 状态筛选：待处理/已同意/已拒绝/已完成
- 表格列：申请编号、用户、商品、类型、原因、申请金额、申请时间、状态、操作
- 同意退款：`PUT /after-sales/admin/{id}/handle` body: `{action:'approve'}`
- 拒绝（需填原因）：`PUT /after-sales/admin/{id}/handle` body: `{action:'reject', remark:'...'}`
- 完成退款：`PUT /after-sales/admin/{id}/refund`

#### 7. 新建 `frontend/src/views/admin/Reviews.vue` — 评价管理（管理后台）
- 路由：`/admin/reviews`
- 按商品名/评分/显示状态筛选：调用 `GET /reviews/admin/list`
- 表格列：商品、用户、评分（星星）、评价内容、图片、时间、显示状态、操作
- 隐藏/显示：`PUT /reviews/admin/{reviewId}/hidden?isHidden=1`
- 回复：`PUT /reviews/admin/{reviewId}/reply`
- 删除：`DELETE /reviews/admin/{reviewId}`

---

## 成员 C — 顾客端会员中心 + 供应商/审计日志

### 职责说明
负责顾客端会员权益相关页面，以及管理后台的供应商管理和审计日志（相对独立、工作量适中的两个页面）。

### 具体任务

#### 1. `frontend/src/views/user/Profile.vue` — 完善个人中心
- 展示会员等级（普通/银卡/金卡/钻石）和当前积分余额：调用 `GET /auth/userinfo`
- 「修改密码」区域：旧密码 + 新密码 + 确认密码，调用 `PUT /auth/password`
- 头像上传（`el-upload`，调用 `PUT /auth/userinfo` 传 `avatarUrl`）
- 性别、生日、邮箱字段编辑和保存：调用 `PUT /auth/userinfo`

#### 2. `frontend/src/views/user/Address.vue` — 完善地址管理
- 「设为默认」按钮：调用 `PUT /addresses`，传 `isDefault: 1`
- 默认地址显示「默认」标签
- 新增地址支持省市区三级联动（静态数据即可）

#### 3. 新建 `frontend/src/views/user/Coupons.vue` — 优惠券中心
- 路由：`/coupons`
- Tab：「领券中心」和「我的优惠券」
- 领券中心：调用 `GET /coupons/available`，每张显示类型/面值/门槛/有效期/剩余数量，点击「立即领取」调用 `POST /coupons/claim/{couponId}`
- 我的优惠券：调用 `GET /coupons/my?status=xxx`，按 Tab 分「未使用/已使用/已过期」

#### 4. 新建 `frontend/src/views/user/PointsLog.vue` — 积分流水
- 路由：`/points-logs`
- 顶部展示当前积分余额：调用 `GET /points/my`
- 积分流水列表：调用 `GET /points/logs`，每条显示变动类型、变动量（正数绿色/负数红色）、变动后余额、时间
- 支持分页，可按 `reason` 筛选

#### 5. 新建 `frontend/src/views/user/Messages.vue` — 站内消息
- 路由：`/messages`
- 顶部「全部已读」按钮：调用 `PUT /messages/read-all`
- 消息列表：调用 `GET /messages/my`，未读消息高亮，点击单条调用 `PUT /messages/{msgId}/read`
- 按消息类型筛选（全部/订单/促销/系统/退款）
- 顶部导航栏未读数角标：调用 `GET /messages/unread-count`

#### 6. 新建 `frontend/src/views/admin/Suppliers.vue` — 供应商管理
- 路由：`/admin/suppliers`
- 表格列：供应商名称、联系人、手机、邮箱、地址、结算账期、操作
- 操作：新增、编辑、删除（有采购单时提示）
- 调用接口：`GET /suppliers/list`、`POST /suppliers/create`、`PUT /suppliers/{supplierId}`、`DELETE /suppliers/{supplierId}`、`GET /suppliers/{supplierId}`（详情）

#### 7. 新建 `frontend/src/views/admin/AuditLog.vue` — 审计日志
- 路由：`/admin/audit-log`
- 只读列表（分页+时间筛选+模块筛选）：调用 `GET /admin/audit-logs?module=xxx`
- 表格列：操作人、操作类型、操作对象、操作时间、IP地址
- 点击行可展开查看操作前后数据快照

---

## 成员 D — 管理后台完善 + 仓储进货端采购管理 + 优惠券管理

### 职责说明
负责管理后台核心管理页面的新建与完善，以及仓储进货端的采购管理功能，以及优惠券配置管理。所有页面使用 `el-table` + `el-dialog` 模式。

### 具体任务

#### 1. `frontend/src/views/admin/Orders.vue` — 完善订单管理
- 订单详情弹窗：商品明细、收货地址、时间线
- 「填写快递发货」操作（待发货订单）：调用 `PUT /admin/orders/{id}/shipping`，body: `{company, trackingNo}`
- 「修改收货地址」操作（待发货订单）：调用 `PUT /admin/orders/{id}/address`，body: `{name, phone, address}`
- 「分配配送员」操作：待发货订单可选配送员，调用 `PUT /admin/deliveries/{deliveryId}/assign?courierId=xxx`
- 多条件筛选：订单号、用户ID、状态、时间范围
- 「强制取消」按钮（需填原因）：调用 `PUT /orders/{orderId}/admin-cancel`

#### 2. `frontend/src/views/admin/Promotions.vue` — 完善促销管理
- 补充「满减活动」Tab
- 创建阶梯满减活动（如满100减15、满200减35），关联全场或指定分类
- 调用接口：`GET /promotions/list`、`POST /promotions/create`、`PUT /promotions/{promotionId}`、`PUT /promotions/{promotionId}/status`
- C端进行中活动展示：`GET /promotions/active`

#### 3. 新建 `frontend/src/views/admin/Admins.vue` — 管理员账号管理
- 路由：`/admin/admins`
- 表格列：用户名、姓名、角色、手机、状态、最后登录时间、操作
- 操作：新增（弹窗填写用户名/姓名/手机/角色/初始密码）、编辑、启用/禁用
- 调用接口：`GET /admin/users`、`POST /admin/users`（如有）、`PUT /admin/users/{id}/status`

#### 4. 新建 `frontend/src/views/admin/Brands.vue` — 品牌管理
- 路由：`/admin/brands`
- 表格列：品牌名、Logo、描述、状态、操作
- 操作：新增、编辑、删除（有商品时后端拒绝，前端展示错误提示）
- 调用接口：`GET /brands/list`、`POST /brands/create`、`PUT /brands/{brandId}`、`DELETE /brands/{brandId}`

#### 5. 新建 `frontend/src/views/admin/Banners.vue` — 轮播图管理
- 路由：`/admin/banners`
- 表格列：图片预览、跳转目标、排序、状态、操作
- 操作：新增（上传图片、填写跳转链接、排序）、编辑、启用/禁用、删除
- 调用接口：`GET /banners/admin/list`、`POST /banners/admin`、`PUT /banners/admin/{bannerId}`、`DELETE /banners/admin/{bannerId}`、`PUT /banners/admin/{bannerId}/toggle`

#### 6. 新建 `frontend/src/views/admin/CouponsManage.vue` — 优惠券管理（管理后台）
- 路由：`/admin/coupons-manage`
- Tab：优惠券列表 / 批量发券
- 优惠券列表（分页+状态筛选）：调用 `GET /coupons/admin/list`
- 表格列：券名、类型、面值/折扣、最低消费、总量/已领/剩余、有效期、状态、操作
- 新增券：弹窗填写名称/类型/面值/门槛/总量/有效期，调用 `POST /coupons/admin`
- 编辑：`PUT /coupons/admin/{couponId}`
- 启用/暂停：`PUT /coupons/admin/{couponId}/status`
- 删除：`DELETE /coupons/admin/{couponId}`
- 批量发券：输入优惠券ID和用户ID列表，调用 `POST /coupons/admin/batch-issue`

#### 7. 新建 `frontend/src/views/admin/PurchaseOrders.vue` — 采购管理（仓储进货端）
- 路由：`/admin/purchase-orders`
- 采购单列表（分页+状态筛选：草稿/已审批/已收货/已取消）：调用 `GET /purchase/list`，可按 supplierId 筛选
- 新增采购申请弹窗：选择供应商、填写预计到货日期、添加商品明细（商品/数量/单价），调用 `POST /purchase/create`
- 采购单详情：调用 `GET /purchase/{poId}`
- 「审批采购单」按钮（草稿状态）：调用 `PUT /purchase/{poId}/approve`
- 「确认到货入库」：填写各商品实际收货数量，调用 `PUT /purchase/{poId}/receive`，自动增加库存
- 「取消采购单」（草稿状态）：调用 `PUT /purchase/{poId}/cancel`

#### 8. `frontend/src/views/admin/Layout.vue` — 侧边栏菜单补充
在现有菜单基础上添加：
- 管理员管理 → `/admin/admins`
- 品牌管理 → `/admin/brands`
- 轮播图管理 → `/admin/banners`
- 优惠券管理 → `/admin/coupons-manage`
- 售后管理 → `/admin/after-sales`
- 评价管理 → `/admin/reviews`
- 秒杀活动 → `/admin/seckill`
- 库存盘点 → `/admin/stocktake`
- 报损记录 → `/admin/damage-records`
- 供应商管理 → `/admin/suppliers`（仓储进货端菜单组）
- 采购管理 → `/admin/purchase-orders`（仓储进货端菜单组）
- 审计日志 → `/admin/audit-log`
- 配送员管理 → `/admin/couriers`

---

## 成员 E — 收银端 + 配送员端 + 数据看板端 + 仓储盘点/报损 + 配送员管理

### 职责说明
负责收银端、配送员端、数据看板端三个独立端口的前端开发，仓储端的盘点和报损页面，以及管理后台配送员管理页面。

### 具体任务

#### 1. 新建 `frontend/src/views/cashier/Cashier.vue` — 收银台主页面（收银端）
- 路由：`/cashier`（独立 Layout，新建 `cashier/Layout.vue`）
- 使用管理员 Token（收银员角色）
- **班次状态栏**：显示当前班次信息，调用 `GET /cashier/shift/current`；无开班则显示「开班」按钮（弹窗填备用金，调用 `POST /cashier/shift/open`）
- **商品搜索区**：输入框实时搜索，调用 `GET /products/list?keyword=xxx` 或 `GET /products/barcode/{barcode}`，结果点击添加到收银清单
- **收银清单**：显示已选商品（名称/单价/数量/小计），可调整数量和删除，底部显示合计金额
- **会员识别**：输入手机号调用 `GET /auth/cashier/member?phone=xxx`，显示等级/积分/可用券数
- **结账弹窗**：支付方式选择（现金/模拟支付）
  - 现金：输入实收金额，自动显示找零，调用 `POST /orders/cashier`
  - 模拟支付：直接调用 `POST /orders/cashier`
- 「清空清单」按钮
- 「交班」按钮：填写清点现金，调用 `POST /cashier/shift/close`，展示交班汇总
- 「历史班次」入口：调用 `GET /cashier/shift/history`

#### 2. 新建 `frontend/src/views/courier/Tasks.vue` — 配送员任务页（配送员端）
- 路由：`/courier`（新建 `courier/Layout.vue`，移动端友好布局）
- 配送员登录：使用 `POST /auth/courier/login`，Token 存入 localStorage（键名 `courierToken`）
- 顶部：显示配送员姓名、今日已送单数，调用 `GET /courier/profile`
- 在线/离线切换：调用 `PUT /courier/status?status=online|offline`
- Tab：待配送 / 历史记录
  - 待配送：调用 `GET /courier/tasks`，任务卡片显示收件人、地址、商品概要
  - 点击展开详情：收件人手机（脱敏，点击复制完整号码）、详细地址、备注、期望配送时间
  - 操作：「已取件」（`PUT /courier/tasks/{taskId}/pickup`）、「已送达」（`PUT /courier/tasks/{taskId}/complete`）、「配送失败」（弹窗填原因，`PUT /courier/tasks/{taskId}/fail`）
  - 历史记录：调用 `GET /courier/tasks/history`
- 个人信息页：姓名/手机/今日单数/累计单数，修改密码（`PUT /courier/password`）

#### 3. `frontend/src/views/admin/Dashboard.vue` — 完善数据看板（数据看板端）
- 安装 ECharts：`npm install echarts`
- **实时经营看板卡片**：
  - 统计概览：调用 `GET /admin/statistics`，展示用户数/商品数/今日订单/今日营收
  - 待处理事项：低库存商品数（点击跳转低库存列表 `GET /products/low-stock`）
- **综合看板**：调用 `GET /admin/dashboard?days=30&topN=10`，展示多维汇总数据
- **商品销售排行榜**：调用 `GET /admin/dashboard/top-products?limit=10`，横向柱状图（ECharts）
- **库存管理入口**：调用 `GET /admin/inventory/logs`，展示近期库存流水
- **财务报表**：调用 `GET /admin/finance`，展示各支付方式收款占比饼图

#### 4. 新建 `frontend/src/views/admin/Seckill.vue` — 秒杀活动管理（管理后台）
- 路由：`/admin/seckill`
- 活动列表（分页）：调用 `GET /seckill/admin/activities`，显示活动名、时间、状态
- 新增活动弹窗：填写活动名称、开始/结束时间，提交调用 `POST /seckill/admin/activities`
- 编辑活动：`PUT /seckill/admin/activities/{seckillId}`
- 活动详情：添加/修改秒杀商品（选择商品、填写秒杀价和秒杀库存），调用 `POST /seckill/admin/activities/{seckillId}/products`

#### 5. 新建 `frontend/src/views/admin/Stocktake.vue` — 库存盘点（仓储进货端）
- 路由：`/admin/stocktake`
- 盘点任务列表（分页+状态筛选）：调用 `GET /stocktake/list`
- 创建盘点弹窗：选择范围（全部/指定分类），提交调用 `POST /stocktake/create`
- 盘点详情：调用 `GET /stocktake/{taskId}`，展示每个商品的账面库存，输入实际数量，有差异的行红色高亮
- 录入实际数量：调用 `PUT /stocktake/{taskId}/input`，body: `{items:[{productId, actualQty}]}`
- 「提交盘点」：调用 `PUT /stocktake/{taskId}/submit`，展示差异汇总报告

#### 6. 新建 `frontend/src/views/admin/DamageRecords.vue` — 报损登记（仓储进货端）
- 路由：`/admin/damage-records`
- 报损记录列表（分页+商品筛选）：调用 `GET /warehouse/damage/list`
- 新增报损弹窗：选择商品、填写数量、选择原因（破损/过期/其他），提交调用 `POST /warehouse/damage`
- 同时可查看库存总览：`GET /warehouse/inventory`
- 低库存预警：`GET /warehouse/inventory/low-stock`
- 库存流水（支持商品/类型/日期筛选）：`GET /warehouse/inventory/logs`

#### 7. 新建 `frontend/src/views/admin/Couriers.vue` — 配送员管理（管理后台）
- 路由：`/admin/couriers`
- 表格列：姓名、手机、状态（在线/离线/禁用）、今日单数、累计单数、操作
- 新增配送员弹窗：填写姓名、手机（作为登录账号）、初始密码，调用 `POST /admin/couriers`
- 启用/禁用操作：调用 `PUT /admin/couriers/{courierId}/status?isDisabled=1|0`
- 配送员列表：调用 `GET /admin/couriers`

---

## 公共约定

### API 调用方式
所有接口调用统一通过 `frontend/src/api/index.js` 封装，不直接写 axios。

### Token 处理
- 顾客端 Token：键名 `token`，存 localStorage
- 管理端/收银端/仓储端/看板端 Token：键名 `adminToken`，存 localStorage
- 配送员 Token：键名 `courierToken`，存 localStorage

### 统一响应格式
```json
{ "code": 200, "message": "success", "data": {} }
```
失败时 code 非 200，前端用 `ElMessage.error(res.message)` 提示。

### 六端路由汇总（组长统一注册）
```
# 顾客端
/products/:id          → user/ProductDetail.vue
/seckill               → user/Seckill.vue
/after-sale            → user/AfterSale.vue
/review/:orderId       → user/Review.vue
/coupons               → user/Coupons.vue
/points-logs           → user/PointsLog.vue
/messages              → user/Messages.vue

# 收银端
/cashier               → cashier/Cashier.vue

# 配送员端
/courier               → courier/Tasks.vue

# 管理后台（含仓储进货端、数据看板端）
/admin/admins          → admin/Admins.vue
/admin/brands          → admin/Brands.vue
/admin/banners         → admin/Banners.vue
/admin/coupons-manage  → admin/CouponsManage.vue
/admin/after-sales     → admin/AfterSales.vue
/admin/reviews         → admin/Reviews.vue
/admin/seckill         → admin/Seckill.vue
/admin/stocktake       → admin/Stocktake.vue
/admin/damage-records  → admin/DamageRecords.vue
/admin/purchase-orders → admin/PurchaseOrders.vue
/admin/suppliers       → admin/Suppliers.vue
/admin/audit-log       → admin/AuditLog.vue
/admin/couriers        → admin/Couriers.vue
```

### 后端接口地址
后端运行在 `http://localhost:8080`，前端 `vite.config.js` 已配置代理，直接用相对路径调用即可。

### 接口路径速查（修正版）

| 功能 | 正确路径 |
|------|----------|
| 积分余额 | `GET /points/my` |
| 积分流水 | `GET /points/logs` |
| 消息列表 | `GET /messages/my` |
| 品牌列表 | `GET /brands/list` |
| 新增品牌 | `POST /brands/create` |
| 秒杀活动列表（C端） | `GET /seckill/activities` |
| 秒杀活动商品（C端） | `GET /seckill/activities/{id}/products` |
| 秒杀管理（B端） | `GET/POST /seckill/admin/activities` |
| 秒杀编辑（B端） | `PUT /seckill/admin/activities/{id}` |
| 秒杀商品配置（B端） | `POST /seckill/admin/activities/{id}/products` |
| 盘点任务列表 | `GET /stocktake/list` |
| 创建盘点 | `POST /stocktake/create` |
| 盘点详情 | `GET /stocktake/{taskId}` |
| 录入盘点数量 | `PUT /stocktake/{taskId}/input` |
| 提交盘点 | `PUT /stocktake/{taskId}/submit` |
| 供应商列表 | `GET /suppliers/list` |
| 新增供应商 | `POST /suppliers/create` |
| 编辑供应商 | `PUT /suppliers/{supplierId}` |
| 删除供应商 | `DELETE /suppliers/{supplierId}` |
| 采购单列表 | `GET /purchase/list` |
| 创建采购单 | `POST /purchase/create` |
| 采购单详情 | `GET /purchase/{poId}` |
| 审批采购单 | `PUT /purchase/{poId}/approve` |
| 确认收货入库 | `PUT /purchase/{poId}/receive` |
| 取消采购单 | `PUT /purchase/{poId}/cancel` |
| 促销活动列表（B端） | `GET /promotions/list` |
| 创建促销活动 | `POST /promotions/create` |
| 编辑促销活动 | `PUT /promotions/{promotionId}` |
| 促销活动状态 | `PUT /promotions/{promotionId}/status` |
| 进行中促销（C端） | `GET /promotions/active` |
| 库存总览 | `GET /warehouse/inventory` |
| 低库存预警 | `GET /warehouse/inventory/low-stock` |
| 库存流水 | `GET /warehouse/inventory/logs` |
| 报损登记 | `POST /warehouse/damage` |
| 报损记录列表 | `GET /warehouse/damage/list` |
| 我的收藏 | `GET /favorites/my` |
| 收藏商品 | `POST /favorites/{productId}` |
| 取消收藏 | `DELETE /favorites/{productId}` |
| 后台发货 | `PUT /admin/orders/{id}/shipping` |
| 后台修改地址 | `PUT /admin/orders/{id}/address` |
| 收银查会员 | `GET /auth/cashier/member?phone=xxx` |
| 配送员个人信息 | `GET /courier/profile` |
| 配送员在线状态 | `PUT /courier/status?status=online\|offline` |