# 前端开发分工文档

> 基于现有后端 API 完成前端页面开发，技术栈：Vue 3 + Element Plus + Pinia + Vue Router + Axios + ECharts
> 覆盖六端：顾客端 / 管理后台 / 收银端 / 仓储进货端 / 数据看板端 / 配送员端

---

## 分工总览

| 成员 | 负责模块 | 新建页面数 | 改造页面数 | 工作量 |
|------|---------|-----------|-----------|--------|
| 组长 A | 基础架构 + 顾客端核心页面完善 | 1 | 3 + 路由 | ⭐⭐⭐ |
| 成员 B | 顾客端订单/售后/评价 + B端售后/评价管理 | 4 | 3 | ⭐⭐⭐⭐⭐ |
| 成员 C | 顾客端会员中心 + 供应商/审计日志 | 5 | 2 | ⭐⭐⭐⭐⭐ |
| 成员 D | 管理后台完善 + 仓储进货端采购管理 | 5 | 3 | ⭐⭐⭐⭐⭐ |
| 成员 E | 收银端 + 配送员端 + 数据看板端 + 仓储盘点/报损 | 6 | 1 | ⭐⭐⭐⭐⭐ |

> 仓储进货端通过 `/admin` 路由以仓管角色访问，数据看板端通过 `/admin` Dashboard 访问，无需独立路由前缀。

---

## 组长 A — 基础架构 + 顾客端核心页面完善

### 职责说明
负责项目整体前端架构维护，完善顾客端最核心的入口页面，并统一注册所有成员新建页面的路由。

### 具体任务

#### 1. `frontend/src/views/user/Home.vue` — 完善首页
- Banner 轮播图：调用 `GET /banners/list`，使用 `el-carousel` 展示
- 「热销商品」区块：调用 `GET /products/list?sortBy=salesCount&sortOrder=desc&pageSize=8`
- 「新品上市」区块：调用 `GET /products/list?sortBy=createTime&sortOrder=desc&pageSize=8`
- 「促销活动入口」卡片：调用 `GET /seckill/list` 展示进行中的秒杀活动
- 修复推荐商品区块，改为调用 `GET /products/recommended`

#### 2. `frontend/src/views/user/Products.vue` — 完善商品列表页
- 添加价格区间筛选（最低价 / 最高价输入框）
- 添加排序方式切换（综合 / 价格升降 / 销量 / 评分）
- 添加品牌筛选下拉框：调用 `GET /brands/list`
- 商品卡片点击跳转到 `/products/:id`

#### 3. 新建 `frontend/src/views/user/ProductDetail.vue` — 商品详情页
- 路由：`/products/:id`
- 展示：商品图片、名称、价格/原价、库存、描述
- SKU 规格选择：调用 `GET /products/{productId}/skus`，价格随规格切换
- 低库存提示（库存 < 预警阈值时显示「仅剩 XX 件」）
- 加入购物车：调用 `POST /cart/add`
- 商品评价列表：调用 `GET /reviews/product/{productId}`
- 同分类相关商品推荐（底部）

#### 4. `frontend/src/views/user/Checkout.vue` — 完善结算页
- 优惠券选择下拉：调用 `GET /coupons/available?orderAmount=xxx`
- 积分抵扣选项（勾选后自动计算最多抵扣 30%）
- 期望配送时间选择（今日/明日，早中晚时段）
- 备注输入框（传入 `remark` 字段）
- 价格明细：商品总价、优惠券减免、积分抵扣、实付金额

#### 5. `frontend/src/router/index.js` — 路由统一注册
所有成员新建页面的路由由组长统一注册：
```
/products/:id          → user/ProductDetail.vue
/after-sale            → user/AfterSale.vue
/review/:orderId       → user/Review.vue
/coupons               → user/Coupons.vue
/points-logs           → user/PointsLog.vue
/messages              → user/Messages.vue
/cashier               → cashier/Cashier.vue
/courier               → courier/Tasks.vue
/admin/admins          → admin/Admins.vue
/admin/brands          → admin/Brands.vue
/admin/banners         → admin/Banners.vue
/admin/after-sales     → admin/AfterSales.vue
/admin/reviews         → admin/Reviews.vue
/admin/seckill         → admin/Seckill.vue
/admin/stocktake       → admin/Stocktake.vue
/admin/damage-records  → admin/DamageRecords.vue
/admin/purchase-orders → admin/PurchaseOrders.vue
/admin/suppliers       → admin/Suppliers.vue
/admin/audit-log       → admin/AuditLog.vue
```

---

## 成员 B — 顾客端订单/售后/评价 + B端售后/评价管理

### 职责说明
负责顾客端订单全流程、售后、评价页面，以及管理后台对应的售后审核和评价管理页面（同一业务域的两侧）。

### 具体任务

#### 1. `frontend/src/views/user/Orders.vue` — 完善订单列表
- 状态 Tab 栏：全部 / 待付款 / 待发货 / 配送中 / 待收货 / 已完成 / 已取消
- 点击 Tab 传入 `status` 参数调用 `GET /orders/list?status=xxx`
- 每条订单显示商品缩略图、订单号、金额、状态标签

#### 2. `frontend/src/views/user/OrderDetail.vue` — 完善订单详情
- 展示完整订单信息：商品明细、收货地址、订单号、下单时间
- 「去支付」按钮（待付款状态）：调用 `POST /orders/{orderId}/pay`
- 「取消订单」按钮（待付款/待发货状态）：调用 `PUT /orders/{orderId}/cancel`
- 配送进度展示：配送员姓名（脱敏电话）和配送状态（已取件/配送中/已送达）
- 「确认收货」按钮（待收货状态）：调用 `PUT /orders/{orderId}/confirm`
- 「再次购买」按钮（已完成状态）：将商品重新加入购物车
- 「申请售后」按钮（已完成状态）：跳转 `/after-sale?orderId=xxx`
- 「去评价」按钮（已完成且未评价时）：跳转 `/review/:orderId`

#### 3. `frontend/src/views/user/Cart.vue` — 完善购物车
- 每个商品前复选框（`el-checkbox`），支持全选/全不选
- 底部价格和件数只统计已勾选商品
- 已下架或缺货商品灰显，禁用勾选和数量修改
- 「去结算」只传已勾选商品 ID 给结算页

#### 4. 新建 `frontend/src/views/user/AfterSale.vue` — 售后申请与进度（顾客端）
- 路由：`/after-sale`
- Tab：「申请售后」和「我的售后」
- 申请表单：售后类型（仅退款/退货退款）、原因文本域、上传凭证图片（`el-upload` 本地预览）
- 提交：调用 `POST /after-sales`
- 我的售后列表：调用 `GET /after-sales/my`，展示状态（待处理/已同意/已拒绝）

#### 5. 新建 `frontend/src/views/user/Review.vue` — 提交评价（顾客端）
- 路由：`/review/:orderId`
- 展示该订单的商品列表
- 每个商品：1~5 星评分（`el-rate`）、文字评价、标签快选（好质量/包装好/物流快等）、匿名开关
- 提交：调用 `POST /reviews`

#### 6. 新建 `frontend/src/views/admin/AfterSales.vue` — 售后退款管理（管理后台）
- 路由：`/admin/after-sales`
- Tab 状态筛选：待处理/已同意/已拒绝/已完成
- 表格列：申请编号、用户、商品、类型、原因、申请金额、申请时间、状态、操作
- 操作：查看凭证图片、同意退款（`PUT /after-sales/admin/{id}/handle` body: `{action:'approve'}`）、拒绝（需填原因）

#### 7. 新建 `frontend/src/views/admin/Reviews.vue` — 评价管理（管理后台）
- 路由：`/admin/reviews`
- 按商品名/评分/显示状态筛选
- 表格列：商品、用户、评分（星星）、评价内容、图片、时间、显示状态、操作
- 操作：隐藏/显示（`PUT /reviews/admin/{id}/hidden`）、回复（`PUT /reviews/admin/{id}/reply`）

---

## 成员 C — 顾客端会员中心 + 供应商/审计日志

### 职责说明
负责顾客端会员权益相关页面，以及管理后台的供应商管理和审计日志（相对独立、工作量适中的两个页面）。

### 具体任务

#### 1. `frontend/src/views/user/Profile.vue` — 完善个人中心
- 展示会员等级（普通/银卡/金卡/钻石）和当前积分余额
- 「修改密码」区域：旧密码 + 新密码 + 确认密码，调用 `PUT /auth/password`
- 头像上传（`el-upload`，调用 `PUT /auth/userinfo` 传 `avatarUrl`）
- 性别、生日、邮箱字段编辑和保存

#### 2. `frontend/src/views/user/Address.vue` — 完善地址管理
- 「设为默认」按钮：调用 `PUT /addresses`，传 `isDefault: 1`
- 默认地址显示「默认」标签
- 新增地址支持省市区三级联动（静态数据即可）

#### 3. 新建 `frontend/src/views/user/Coupons.vue` — 优惠券中心
- 路由：`/coupons`
- Tab：「领券中心」和「我的优惠券」
- 领券中心：调用 `GET /coupons/available`，每张显示类型/面值/门槛/有效期/剩余数量，点击「立即领取」调用 `POST /coupons/claim/{couponId}`
- 我的优惠券：调用 `GET /coupons/my?status=xxx`，按 Tab 分「未使用/已使用/已过期」

#### 4. 新建 `frontend/src/views/user/PointsLog.vue` — 积分流水
- 路由：`/points-logs`
- 顶部展示当前积分余额（从 Pinia userStore 读取）
- 列表：调用 `GET /users/points-logs`，每条显示变动类型、变动量（正数绿色/负数红色）、变动后余额、时间
- 支持分页

#### 5. 新建 `frontend/src/views/user/Messages.vue` — 站内消息
- 路由：`/messages`
- 顶部「全部已读」按钮：调用 `PUT /messages/read-all`
- 消息列表：调用 `GET /messages`，未读消息高亮，点击单条调用 `PUT /messages/{msgId}/read`
- 按消息类型筛选（全部/订单/促销/系统/退款）
- 顶部导航栏未读数角标：调用 `GET /messages/unread-count`

#### 6. 新建 `frontend/src/views/admin/Suppliers.vue` — 供应商管理
- 路由：`/admin/suppliers`
- 表格列：供应商名称、联系人、手机、邮箱、地址、结算账期、操作
- 操作：新增、编辑、删除（有采购单时提示）
- 调用接口：`GET/POST/PUT/DELETE /admin/suppliers`

#### 7. 新建 `frontend/src/views/admin/AuditLog.vue` — 审计日志
- 路由：`/admin/audit-log`
- 只读列表（分页+时间筛选+操作人筛选）：调用 `GET /admin/audit-logs`
- 表格列：操作人、操作类型、操作对象、操作时间、IP地址
- 点击行可展开查看操作前后数据快照

---

## 成员 D — 管理后台完善 + 仓储进货端采购管理

### 职责说明
负责管理后台核心管理页面的新建与完善，以及仓储进货端的采购管理功能。所有页面使用 `el-table` + `el-dialog` 模式。

### 具体任务

#### 1. `frontend/src/views/admin/Orders.vue` — 完善订单管理
- 订单详情弹窗：商品明细、收货地址、时间线
- 「分配配送员」操作：待发货订单可选配送员，调用 `PUT /admin/deliveries/{deliveryId}/assign`
- 多条件筛选：订单号、用户手机号、状态、时间范围、支付方式
- 「强制取消」按钮（需填原因）：调用 `PUT /orders/{orderId}/admin-cancel`

#### 2. `frontend/src/views/admin/Promotions.vue` — 完善促销管理
- 补充「满减活动」Tab
- 创建阶梯满减活动（如满100减15、满200减35），关联全场或指定分类
- 调用接口：`GET/POST/PUT/DELETE /admin/promotions`（满减部分）

#### 3. 新建 `frontend/src/views/admin/Admins.vue` — 管理员账号管理
- 路由：`/admin/admins`
- 表格列：用户名、姓名、角色、手机、状态、最后登录时间、操作
- 操作：新增（弹窗填写用户名/姓名/手机/角色/初始密码）、编辑、启用/禁用、重置密码
- 调用接口：`GET/POST/PUT /admin/admins`，`PUT /admin/admins/{id}/status`，`PUT /admin/admins/{id}/reset-password`

#### 4. 新建 `frontend/src/views/admin/Brands.vue` — 品牌管理
- 路由：`/admin/brands`
- 表格列：品牌名、Logo、描述、排序、状态、操作
- 操作：新增、编辑、删除（有商品时后端拒绝，前端展示错误提示）
- 调用接口：`GET/POST/PUT/DELETE /admin/brands`

#### 5. 新建 `frontend/src/views/admin/Banners.vue` — 轮播图管理
- 路由：`/admin/banners`
- 表格列：图片预览、跳转目标、排序、状态、操作
- 操作：新增（上传图片、填写跳转链接、排序）、编辑、启用/禁用、删除
- 调用接口：`GET/POST/PUT/DELETE /admin/banners`，`PUT /admin/banners/{id}/toggle`

#### 6. 新建 `frontend/src/views/admin/PurchaseOrders.vue` — 采购管理（仓储进货端）
- 路由：`/admin/purchase-orders`
- 采购单列表（分页+状态筛选：草稿/已审批/已收货/已取消）
- 新增采购申请弹窗：选择供应商、填写预计到货日期、添加商品明细（商品/数量/单价）
- 采购单详情：查看明细、供应商信息、审批状态
- 「确认到货入库」：填写各商品实际收货数量，调用 `PUT /admin/purchase-orders/{id}/receive`，自动增加库存
- 「取消采购单」（草稿状态）：调用 `PUT /admin/purchase-orders/{id}/cancel`
- 调用接口：`GET/POST /admin/purchase-orders`，`PUT /admin/purchase-orders/{id}/receive`

#### 7. `frontend/src/views/admin/Layout.vue` — 侧边栏菜单补充
在现有菜单基础上添加：
- 管理员管理 → `/admin/admins`
- 品牌管理 → `/admin/brands`
- 轮播图管理 → `/admin/banners`
- 售后管理 → `/admin/after-sales`
- 评价管理 → `/admin/reviews`
- 秒杀活动 → `/admin/seckill`
- 库存盘点 → `/admin/stocktake`
- 报损记录 → `/admin/damage-records`
- 供应商管理 → `/admin/suppliers`（仓储进货端菜单组）
- 采购管理 → `/admin/purchase-orders`（仓储进货端菜单组）
- 审计日志 → `/admin/audit-log`

---

## 成员 E — 收银端 + 配送员端 + 数据看板端 + 仓储盘点/报损

### 职责说明
负责收银端、配送员端、数据看板端三个独立端口的前端开发，以及仓储端的盘点和报损页面。

### 具体任务

#### 1. 新建 `frontend/src/views/cashier/Cashier.vue` — 收银台主页面（收银端）
- 路由：`/cashier`（独立 Layout，新建 `cashier/Layout.vue`）
- 使用管理员 Token（收银员角色）
- 页面三区域：
  - **左上：班次状态栏** — 显示当前班次信息（开始时间、今日订单数、现金合计、模拟支付合计），无开班则显示「开班」按钮（弹窗填备用金，调用 `POST /cashier/shift/open`）
  - **中：商品搜索区** — 输入框实时搜索（调用 `GET /cashier/products/search?keyword=xxx`），结果列表点击添加到收银清单
  - **右：收银清单** — 显示已选商品（名称/单价/数量/小计），可调整数量和删除，底部显示合计金额
- **会员识别**（K-08~K-09）：输入手机号查找会员，显示等级/积分/可用券数，可选择优惠券抵扣
- 结账弹窗：支付方式选择（现金/模拟支付）
  - 现金：输入实收金额，自动显示找零，调用 `POST /orders/cashier`
  - 模拟支付：直接调用 `POST /orders/cashier`
- 「清空清单」按钮（K-15）
- 「交班」按钮：填写清点现金，调用 `POST /cashier/shift/close`，展示交班汇总
- 「历史班次」入口：调用 `GET /cashier/shift/history`
- 「查询历史订单」（K-13）：输入订单号查找，用于处理退款
- 「收银台退款」（K-14）：选择退货商品和数量，提交后库存回滚

#### 2. 新建 `frontend/src/views/courier/Tasks.vue` — 配送员任务页（配送员端）
- 路由：`/courier`（新建 `courier/Layout.vue`，移动端友好布局）
- 配送员登录：使用 `POST /auth/courier/login`，Token 存入 localStorage
- 顶部：显示配送员姓名、今日已送单数（调用 `GET /courier/info`）
- Tab：待配送 / 历史记录
  - 待配送：调用 `GET /courier/tasks?status=pending`，任务卡片显示收件人、地址、商品概要
  - 点击展开详情：收件人手机（脱敏，点击复制完整号码）、详细地址、备注、期望配送时间
  - 操作：「已取件」（`PUT /courier/tasks/{taskId}/pickup`）、「已送达」（`PUT /courier/tasks/{taskId}/complete`）、「配送失败」（弹窗填原因，`PUT /courier/tasks/{taskId}/fail`）
  - 历史记录：调用 `GET /courier/tasks/history`
- 个人信息页：姓名/手机/今日单数/累计单数，修改密码（`PUT /courier/password`）

#### 3. `frontend/src/views/admin/Dashboard.vue` — 完善数据看板（数据看板端）
- 安装 ECharts：`npm install echarts`
- **实时经营看板卡片**（D-01~D-04）：
  - 今日销售额（含环比昨日±%）、今日订单数、今日客单价
  - 本月累计销售额、订单数、退款金额
  - 待处理事项：待发货订单数、待审核退款数、待审批采购单数（点击跳转）
  - 低库存预警商品数（点击跳转低库存列表）
- **销售分析图表**（D-06~D-10）：
  - 销售趋势折线图：调用 `GET /admin/statistics/sales-trend?type=day`，支持日/周/月切换，双轴（销售额+订单量）
  - 商品销售排行榜：调用 `GET /admin/statistics/product-rank?limit=10`，横向柱状图
  - 分类销售占比：调用 `GET /admin/statistics/category-sales`，饼图
  - 24小时时段分析：调用 `GET /admin/statistics/hourly-sales`，柱状图
- **用户分析**（D-12~D-13）：
  - 新增用户趋势折线图：调用 `GET /admin/statistics/user-trend`
- **营销效果**（D-18）：
  - 优惠券核销统计：调用 `GET /admin/statistics/coupon-stats`，漏斗图/柱状图

#### 4. 新建 `frontend/src/views/admin/Seckill.vue` — 秒杀活动管理
- 路由：`/admin/seckill`
- 活动列表（分页）：调用 `GET /admin/seckill`，显示活动名、时间、状态
- 新增活动弹窗：填写活动名称、开始/结束时间，提交调用 `POST /admin/seckill`
- 活动详情：添加/删除秒杀商品（选择商品、填写秒杀价和秒杀库存）
- 状态操作：开始/暂停/结束（调用 `PUT /admin/seckill/{id}/status`）

#### 5. 新建 `frontend/src/views/admin/Stocktake.vue` — 库存盘点（仓储进货端）
- 路由：`/admin/stocktake`
- 盘点任务列表（分页+状态筛选）：调用 `GET /admin/stocktake`
- 创建盘点弹窗：选择范围（全部/指定分类），提交调用 `POST /admin/stocktake`
- 盘点详情：展示每个商品的账面库存，输入实际数量，有差异的行红色高亮
- 「提交盘点」：调用 `PUT /admin/stocktake/{taskId}/submit`，展示差异汇总报告

#### 6. 新建 `frontend/src/views/admin/DamageRecords.vue` — 报损登记（仓储进货端）
- 路由：`/admin/damage-records`
- 报损记录列表（分页+时间筛选）：调用 `GET /admin/damage-records`
- 新增报损弹窗：选择商品、填写数量、选择原因（破损/过期/其他），提交调用 `POST /admin/damage-records`

---

## 公共约定

### API 调用方式
所有接口调用统一通过 `frontend/src/api/index.js` 封装，不直接写 axios。

### API 模块速查（按成员）

各成员在自己的 `.vue` 文件顶部按需 import，不需要的不用引入。

**组长 A**（已完成）
```js
import { bannerAPI, productAPI, seckillAPI, cartAPI, brandAPI, reviewAPI, couponAPI, orderAPI, addressAPI } from '@/api'
```

**成员 B** — 订单/售后/评价
```js
import { orderAPI, cartAPI, afterSaleAPI, reviewAPI } from '@/api'
```
- `orderAPI.getList / getDetail / cancel / confirm / pay` — 订单列表与操作
- `cartAPI.getList / updateQuantity / remove` — 完善购物车
- `afterSaleAPI.create / getMy / adminGetList / adminHandle` — 售后申请与审核
- `reviewAPI.create / getByProduct / adminToggleHidden / adminReply` — 评价提交与管理

**成员 C** — 会员中心/供应商/审计日志
```js
import { authAPI, addressAPI, couponAPI, pointsAPI, messageAPI, adminAPI } from '@/api'
```
- `authAPI.updateUserInfo / changePassword` — 个人中心修改
- `addressAPI` — 地址管理（设默认用 `update`）
- `couponAPI.getAvailable / getMyCoupons / claim` — 优惠券中心
- `pointsAPI.getLogs` — 积分流水
- `messageAPI.getList / getUnreadCount / markRead / markAllRead` — 站内消息
- `adminAPI.getSuppliers / createSupplier / updateSupplier / deleteSupplier / getAuditLogs` — 供应商与审计日志

**成员 D** — 管理后台完善/采购管理
```js
import { adminAPI, orderAPI, brandAPI, bannerAPI, courierAPI } from '@/api'
```
- `adminAPI.getOrders` + `orderAPI.adminCancel` — 订单管理
- `adminAPI.getAdmins / createAdmin / updateAdmin / updateAdminStatus / resetAdminPassword` — 管理员账号
- `adminAPI.getPromotions / createPromotion / updatePromotion / deletePromotion` — 促销管理
- `brandAPI.adminGetList / create / update / delete` — 品牌管理
- `bannerAPI.adminGetList / create / update / toggle / delete` — 轮播图管理
- `adminAPI.getPurchaseOrders / createPurchaseOrder / receivePurchaseOrder / cancelPurchaseOrder` — 采购管理
- `courierAPI.adminAssign` — 分配配送员

**成员 E** — 收银端/配送员端/数据看板/盘点报损
```js
import { cashierAPI, courierAPI, orderAPI, seckillAPI, stocktakeAPI, adminAPI } from '@/api'
```
- `cashierAPI.openShift / getCurrentShift / closeShift / getHistory / searchProduct` — 收银班次
- `orderAPI.cashierCreate` — 收银台下单
- `courierAPI.getInfo / changePassword / getTasks / pickup / complete / fail / getHistory` — 配送员端
- `adminAPI.getSalesTrend / getProductRank / getCategorySales / getHourlySales / getUserTrend / getCouponStats` — 数据看板图表
- `seckillAPI.adminGetList / create / updateStatus / addProduct / removeProduct` — 秒杀活动
- `stocktakeAPI.create / getList / getDetail / updateItems / submit` — 库存盘点
- `adminAPI.getDamageRecords / createDamageRecord` — 报损登记

### Token 处理
- 顾客端 Token：键名 `token`，存 localStorage
- 管理端/收银端/仓储端/看板端 Token：键名 `adminToken`，存 localStorage
- 配送员 Token：键名 `courierToken`，存 localStorage

### 统一响应格式
```json
{ "code": 200, "msg": "success", "data": {} }
```
失败时 code 非 200，前端用 `ElMessage.error(res.msg)` 提示。

### 六端路由汇总（组长统一注册）
```
# 顾客端
/products/:id          → user/ProductDetail.vue
/after-sale            → user/AfterSale.vue
/review/:orderId       → user/Review.vue
/coupons               → user/Coupons.vue
/points-logs           → user/PointsLog.vue
/messages              → user/Messages.vue

# 收银端
/cashier               → cashier/Cashier.vue

# 配送员端
/courier               → courier/Tasks.vue

# 管理后台（含仓储进货端、数据看板端）
/admin/admins          → admin/Admins.vue
/admin/brands          → admin/Brands.vue
/admin/banners         → admin/Banners.vue
/admin/after-sales     → admin/AfterSales.vue
/admin/reviews         → admin/Reviews.vue
/admin/seckill         → admin/Seckill.vue
/admin/stocktake       → admin/Stocktake.vue
/admin/damage-records  → admin/DamageRecords.vue
/admin/purchase-orders → admin/PurchaseOrders.vue
/admin/suppliers       → admin/Suppliers.vue
/admin/audit-log       → admin/AuditLog.vue
```

### 后端接口地址
后端运行在 `http://localhost:8080`，前端 `vite.config.js` 已配置代理，直接用相对路径调用即可。
