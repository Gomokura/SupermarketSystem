# 前端页面完善计划（除 B 外）— 100% 实现

## 📋 实现优先级和工作量

### Phase 1: 关键页面（影响演示效果）⭐⭐⭐⭐⭐

#### 组长 A（5 个页面）— 2-3 天
```
1. Home.vue           ← 首页，需要 Banner、热销、新品、活动入口
2. Products.vue       ← 商品列表，需要筛选、排序
3. ProductDetail.vue  ← 商品详情，需要 SKU、加购、评价
4. Seckill.vue        ← 秒杀活动列表
5. Checkout.vue       ← 结算页面，需要优惠券、积分、配送时间

关键接口依赖:
✓ GET /banners/list
✓ GET /products/list
✓ GET /products/{id}
✓ GET /products/{id}/skus
✓ GET /reviews/product/{id}
✓ POST /cart/add
✓ GET /seckill/activities
✓ GET /coupons/available
```

#### 成员 E（9 个页面）— 3-4 天
```
1. Cashier.vue        ← 收银台（最复杂，涉及班次、搜索、支付）
2. Tasks.vue          ← 配送任务（移动端，需要状态管理）
3. Dashboard.vue      ← 数据看板（需要 ECharts 图表）
4. Seckill(admin).vue ← 秒杀管理
5. Stocktake.vue      ← 库存盘点
6. DamageRecords.vue  ← 报损登记
7. Couriers.vue       ← 配送员管理
8. Cashier/Layout.vue ← 收银端路由
9. Courier/Layout.vue ← 配送端路由

关键接口依赖:
✓ POST /cashier/shift/open
✓ GET /products/list
✓ POST /orders/cashier
✓ GET /courier/tasks
✓ PUT /courier/tasks/{id}/pickup
✓ ECharts 图表库
```

### Phase 2: 管理后台（高优先级）— 2-3 天

#### 成员 D（8 个页面）
```
1. Admins.vue              ← 管理员管理
2. Brands.vue              ← 品牌管理
3. Banners.vue             ← 轮播图管理
4. CouponsManage.vue       ← 优惠券管理
5. PurchaseOrders.vue      ← 采购管理（仓储）
6. Orders(admin).vue       ← 订单管理（B端）
7. Promotions.vue          ← 促销活动管理
8. Layout.vue              ← 侧边栏菜单

关键接口依赖:
✓ GET /brands/list
✓ POST /brands/create
✓ GET /banners/admin/list
✓ GET /coupons/admin/list
✓ GET /purchase/list
✓ POST /purchase/create
```

### Phase 3: 顾客端会员中心（中优先级）— 1-2 天

#### 成员 C（7 个页面）
```
1. Profile.vue        ← 个人中心
2. Address.vue        ← 地址管理
3. Coupons.vue        ← 优惠券中心
4. PointsLog.vue      ← 积分流水
5. Messages.vue       ← 站内消息
6. Suppliers.vue      ← 供应商管理
7. AuditLog.vue       ← 审计日志

关键接口依赖:
✓ GET /auth/userinfo
✓ GET /coupons/my
✓ GET /points/logs
✓ GET /messages/my
✓ GET /suppliers/list
✓ GET /audit-logs
```

---

## 🚀 开始实现

**总工作量**: 8-10 天（29 个页面）

### 实现步骤

1. Phase 1（Day 1-3): 实现组长A + 成员E关键页面
2. Phase 2（Day 4-6): 实现成员D管理后台页面  
3. Phase 3（Day 7-8): 实现成员C会员中心页面
4. 测试与优化（Day 9-10）

---

## ✅ 立即开始

**我从 Phase 1 开始，逐个页面实现：**

### 实现顺序：
1. ✅ **Cashier.vue** ← 最复杂，收银端核心
2. ✅ **Home.vue** ← 首页，首先呈现给用户
3. ✅ **Dashboard.vue** ← 数据看板，需要 ECharts
4. ✅ **PurchaseOrders.vue** ← 采购管理
5. ✅ 其他页面逐步补充...

---

准备开始实现？还是要调整优先级？
