# 超市管理系统 - 开发日志

> 项目：超市管理系统（SupermarketSystem）
> 技术栈：Vue 3 + Spring Boot 3.2 + Oracle 19c
> 项目性质：企业实训课程项目

---

## 2026-03-21 — 架构重构：JSP/Servlet → Vue 3 + Spring Boot

**提交：** `536a50e` feat: Vue3 + Spring Boot 前后端分离，文档与工作日志更新
**作者：** lqt（Gomokura）

### 背景

在队友（li-xibei1231）完成的 JSP + Servlet 传统架构基础上，负责将整个系统重构为现代化的 **Vue 3 + Spring Boot 前后端分离架构**，并同步更新所有项目文档。

---

### 一、后端重构（Spring Boot 3.2）

新建 `backend/` 模块，从零搭建 Spring Boot 后端：

| 层级 | 文件数 | 内容 |
|------|--------|------|
| 配置类 | 4 | CorsConfig（跨域）、JwtConfig（JWT工具）、WebConfig（拦截器注册） |
| 拦截器 | 1 | JwtInterceptor — 统一 JWT 认证 |
| 控制器 | 6 | AuthController、ProductController、CartController、OrderController、AddressController、AdminController |
| 服务层 | 5 | AuthService、ProductService、CartService、OrderService、AdminService |
| 实体类 | 15 | User、Product、Category、Order、OrderItem、Cart、Address、Payment、Delivery、Promotion、InventoryLog、PurchaseOrder、Supplier、AuditLog 等 |
| Mapper | 14 | 对应各实体的 MyBatis Plus 接口 |
| DTO | 4 | LoginRequest、RegisterRequest、CreateOrderRequest、CartRequest |
| 通用类 | 2 | Result（统一响应封装）、PageRequest（分页请求） |

**实现的 REST API 接口（45+）：**

| 模块 | 接口数 | 代表接口 |
|------|--------|----------|
| 认证 | 3 | POST /api/auth/login、POST /api/auth/register、GET /api/auth/userinfo |
| 商品 | 9 | GET /api/products/list、商品 CRUD、分类管理 |
| 购物车 | 5 | GET /api/cart/list、POST /api/cart/add、PUT /api/cart/update、DELETE /api/cart/{id}、DELETE /api/cart/clear |
| 订单 | 4 | POST /api/orders/create、GET /api/orders/list、GET /api/orders/{id}、PUT /api/orders/cancel/{id} |
| 地址 | 4 | 地址 CRUD |
| 管理端 | 20+ | 用户管理、库存入出库、配送、促销、财务报表、统计数据 |

**配置文件：**
- `backend/pom.xml` — Maven 依赖（Spring Boot 3.2、MyBatis Plus、JWT、Oracle JDBC）
- `backend/src/main/resources/application.yml` — 数据库连接、JWT 配置

---

### 二、负责的核心业务模块

#### 购物车模块

- `CartController.java` + `CartService.java` + `CartMapper.java`
- 前端：`frontend/src/views/user/Cart.vue`
- 功能：添加商品、修改数量、删除商品、清空购物车、实时计算小计

#### 订单模块（含完整事务处理）

- `OrderController.java` + `OrderService.java` + `OrderMapper.java` + `OrderItemMapper.java`
- 前端：`user/Orders.vue`、`user/OrderDetail.vue`、`user/Checkout.vue`
- 订单事务流程：
  ```
  创建订单 → 扣减库存 → 记录库存日志 → 清空购物车 → 生成支付记录
  ```

#### 支付模块

- `PaymentMapper.java` + `Payment.java`
- 支持支付方式：微信支付 / 支付宝 / 银行卡 / 货到付款

#### JWT 认证模块

- `JwtConfig.java` — Token 生成与验证工具
- `JwtInterceptor.java` — 全局请求拦截，自动验证 Token
- `AuthController.java` + `AuthService.java` — 登录、注册、获取用户信息
- 认证流程：
  ```
  用户登录 → 生成 JWT Token → 前端存储 → 请求携带 Bearer Token → 拦截器验证 → Token过期自动跳转登录
  ```

---

### 三、前端搭建（Vue 3）

新建 `frontend/` 模块，完整搭建 Vue 3 前端工程：

**工程配置：**
- `package.json` — 依赖管理（Vue 3、Element Plus、Pinia、Vue Router 4、Axios、Vite）
- `vite.config.js` — 开发代理配置（将 `/api` 转发至 `localhost:8080`）
- `src/utils/request.js` — Axios 封装，统一请求头、响应拦截、Token 注入
- `src/api/index.js` — 所有接口的统一封装
- `src/stores/user.js` — Pinia 用户状态管理
- `src/router/index.js` — Vue Router 路由配置（含路由守卫）

**用户端页面（10个）：**

| 页面 | 文件 | 功能 |
|------|------|------|
| 登录/注册 | Login.vue | 用户登录、注册 |
| 用户布局 | Layout.vue | 顶部导航、路由出口 |
| 首页 | user/Home.vue | 首页展示 |
| 商品列表 | user/Products.vue | 分类浏览、关键词搜索 |
| 购物车 | user/Cart.vue | 购物车管理 |
| 结算页 | user/Checkout.vue | 选地址、选支付方式、提交订单 |
| 我的订单 | user/Orders.vue | 订单列表、取消订单 |
| 订单详情 | user/OrderDetail.vue | 订单详细信息 |
| 收货地址 | user/Address.vue | 地址增删改查 |
| 个人中心 | user/Profile.vue | 个人信息管理 |

**管理端页面（11个）：**

| 页面 | 文件 | 功能 |
|------|------|------|
| 管理布局 | admin/Layout.vue | 侧边菜单、路由出口 |
| 数据概览 | admin/Dashboard.vue | 统计数据看板 |
| 商品管理 | admin/Products.vue | 商品 CRUD、上下架 |
| 分类管理 | admin/Categories.vue | 商品分类管理 |
| 用户管理 | admin/Users.vue | 用户账号管理 |
| 订单管理 | admin/Orders.vue | 查看所有订单 |
| 库存管理 | admin/Inventory.vue | 入库/出库操作 |
| 配送管理 | admin/Deliveries.vue | 分配配送员、状态跟踪 |
| 促销管理 | admin/Promotions.vue | 促销活动管理 |
| 财务报表 | admin/Finance.vue | 营收统计 |

---

### 四、文档更新

同步更新以下全部文档以适配新架构：

| 文档 | 更新内容 |
|------|----------|
| README.md | 技术栈、项目结构、部署步骤全面更新 |
| 快速部署指南.md | 改为前后端分离部署流程 |
| 项目总结.md | 更新完成情况与评分对照 |
| 项目完成报告.md | 架构升级说明与技术亮点 |
| 项目检查清单.md | 文件清单与功能检查更新 |
| docs/设计文档.md | 数据库设计、API 设计更新 |
| docs/用户手册.md | 操作说明更新 |
| docs/测试文档.md | 测试环境与接口测试更新 |
| docs/开发文档.md | 开发指南更新 |

---

### 五、代码统计

| 类型 | 文件数 | 代码量 |
|------|--------|--------|
| Java 后端 | 50+ | 约 3000 行 |
| Vue 前端 | 25+ | 约 2500 行 |
| 配置文件 | 8 | 约 200 行 |
| **合计** | **80+** | **约 5500 行** |

---

### 六、待办事项

- [ ] 后端 API 接口联调测试
- [ ] 前端页面功能测试
- [ ] 前后端完整联调
- [ ] 数据库数据验证

---

## 运行方式

```bash
# 1. 初始化 Oracle 数据库（按序执行 database/ 目录下的 SQL 脚本）

# 2. 配置数据库连接
# 编辑 backend/src/main/resources/application.yml

# 3. 启动后端（端口 8080）
cd backend && mvn spring-boot:run

# 4. 启动前端（端口 3000）
cd frontend && npm install && npm run dev
```

默认账户：`admin / admin123`（管理员）、`user01 / user123`（普通用户）
