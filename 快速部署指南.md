# 超市管理系统 - 快速部署指南

## 技术架构

本系统采用 **前后端分离** 架构：
- **前端**: Vue 3 + Vite + Element Plus（端口 3000）
- **后端**: Spring Boot 3.2 + MyBatis Plus（端口 8080）
- **数据库**: Oracle Database

---

## 快速部署（3步完成）

### 第1步：初始化数据库

双击运行项目根目录下的 `init_database.bat`

脚本会自动：
- 连接到 Oracle 数据库
- 创建所有表结构
- 插入初始测试数据

**预计时间：1分钟**

---

### 第2步：启动后端

```bash
cd backend
mvn spring-boot:run
```

或使用 IDE 直接运行 `SupermarketBackendApplication.java`

**预计时间：30秒**

---

### 第3步：启动前端

```bash
cd frontend
npm install
npm run dev
```

**预计时间：2分钟（首次）**

---

## 详细步骤

### 步骤1：数据库初始化

1. 确保 Oracle 服务已启动
2. 双击运行 `init_database.bat`
3. 看到以下信息表示成功：

```
Table created.
Sequence created.
1 row created.
Commit complete.
```

---

### 步骤2：后端配置

编辑 `backend/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:XE
    username: system
    password: 123456
```

---

### 步骤3：运行后端

使用 Maven：

```bash
cd backend
mvn spring-boot:run
```

或打包后运行：

```bash
mvn clean package
java -jar target/supermarket-backend-1.0.0.jar
```

---

### 步骤4：运行前端

```bash
cd frontend
npm install
npm run dev
```

---

## 访问系统

打开浏览器访问：**http://localhost:3000**

---

## 测试账户

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 普通用户 | user01 | user123 |

---

## 一键启动脚本

创建 `start-all.bat`：

```batch
@echo off
echo Starting Backend...
start cmd /k "cd backend && mvn spring-boot:run"

echo Waiting for backend to start...
timeout /t 15

echo Starting Frontend...
start cmd /k "cd frontend && npm run dev"

echo.
echo All services started!
echo Backend: http://localhost:8080
echo Frontend: http://localhost:3000
pause
```

---

## 常见问题

### 问题1：后端启动报错 "数据库连接失败"
**解决**：
1. 确保 Oracle 服务已启动
2. 检查 `application.yml` 中的连接信息
3. 确认用户名密码正确

### 问题2：前端无法访问后端 API
**解决**：
1. 检查后端是否正常运行（端口 8080）
2. 检查 `vite.config.js` 中的代理配置

### 问题3：npm install 失败
**解决**：
1. 使用管理员权限运行
2. 切换到淘宝镜像：`npm config set registry https://registry.npmmirror.com`
3. 清理缓存：`npm cache clean --force`

### 问题4：跨域错误 (CORS)
**解决**：后端已配置 CORS，如仍有问题检查：
1. 浏览器控制台错误信息
2. 后端控制台日志
3. Network 面板检查请求头

---

## 目录结构

```
SupermarketSystem-master/
├── backend/                 # Spring Boot 后端
│   ├── src/main/java/
│   │   └── com/supermarket/
│   │       ├── controller/  # REST API 控制器
│   │       ├── service/     # 业务逻辑层
│   │       ├── mapper/      # 数据访问层
│   │       ├── entity/      # 实体类
│   │       ├── config/      # 配置类
│   │       └── interceptor/ # 拦截器
│   └── src/main/resources/
│       └── application.yml  # 配置文件
│
├── frontend/               # Vue 3 前端
│   ├── src/
│   │   ├── api/           # API 接口
│   │   ├── router/        # 路由
│   │   ├── stores/        # 状态管理
│   │   └── views/         # 页面组件
│   └── vite.config.js     # Vite 配置
│
└── database/              # 数据库脚本
```

---

## 联系方式

如遇问题，请查看：
1. 后端控制台日志
2. 浏览器开发者工具（F12）
3. 网络请求面板

祝使用愉快！
