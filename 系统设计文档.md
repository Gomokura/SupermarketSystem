# 超市管理系统 — 系统设计文档 v3.0

> 技术栈：Vue 3 + Spring Boot 3.2 + MyBatis-Plus + Oracle XE
> 更新日期：2026-05-05
> 适用范围：后端 API 参考 / 前端队员接口对接

---

## 目录

1. [项目架构概览](#1-项目架构概览)
2. [技术栈说明](#2-技术栈说明)
3. [六端功能分工](#3-六端功能分工)
4. [数据库设计（28张表）](#4-数据库设计)
5. [统一接口规范](#5-统一接口规范)
6. [API 接口设计](#6-api-接口设计)
7. [项目目录结构](#7-项目目录结构)
8. [前端开发指引](#8-前端开发指引)

---

## 1. 项目架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                      前端层（Vue 3）                          │
│  顾客端  │  管理后台  │  收银端  │  配送员端                   │
└──────────────────────────┬──────────────────────────────────┘
                           │  HTTP / JSON（Axios）
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  后端层（Spring Boot 3.2）                    │
│  JWT拦截器 → Controller（17个）→ Service → Mapper（28个）     │
└──────────────────────────┬──────────────────────────────────┘
                           │  MyBatis-Plus / JDBC
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据库（Oracle XE）                        │
│              28张业务表 + Sequence主键 + 索引                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 技术栈说明

### 后端

| 组件 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 3.2 | 主框架 |
| MyBatis-Plus | 3.5.x | ORM，BaseMapper + LambdaQueryWrapper |
| Oracle JDBC | ojdbc11 | 数据库驱动 |
| JWT | jjwt 0.11 | 无状态身份认证 |
| Maven | 3.x | 构建工具 |

### 前端

| 组件 | 版本 | 说明 |
|------|------|------|
| Vue | 3.x | 主框架，Composition API |
| Element Plus | 2.x | UI 组件库 |
| Pinia | 2.x | 状态管理 |
| Vue Router | 4.x | 路由 |
| Axios | 1.x | HTTP 客户端 |
| ECharts | 5.x | 数据可视化图表 |
| Vite | 5.x | 构建工具 |

---

## 3. 六端功能分工

| 端 | 面向用户 | 访问方式 | 核心定位 |
|----|---------|---------|----------|
| 顾客端（C端） | 普通消费者 | Web / 移动H5 | 商品浏览、购物下单、订单追踪、会员互动 |
| 管理后台（B端） | 管理员/店长/财务/客服 | PC浏览器 `/admin` | 系统全局管理，商品/订单/用户/营销/报表 |
| 收银端 | 收银员 | PC专用页面 `/cashier` | 线下快速收银、班次管理、会员识别 |
| 仓储进货端 | 仓库管理员 | PC/平板 `/admin`（仓管角色） | 采购入库、库存盘点、报损管理 |
| 数据看板端 | 店长/老板 | PC大屏/手机 `/admin` Dashboard | 实时经营数据、销售趋势、库存健康度 |
| 配送员端 | 配送员 | 手机H5 `/courier` | 查看配送任务、更新配送状态、联系顾客 |

---

## 4. 数据库设计

### 数据库脚本

执行 `database/all_tables.sql`（v3.0 完整版，唯一有效脚本）：

```bash
sqlplus system/Liu12345@localhost:1521/ORCL @database/all_tables.sql
# 或使用 Python 脚本（解决编码问题）：
python database/run_sql.py
```

### 28张表清单

#### 用户相关（3张）

| 表名 | 说明 | 主键序列 |
|------|------|----------|
| users | 普通用户 | seq_user_id |
| admins | 管理员账号（含角色） | seq_admin_id |
| couriers | 配送员账号 | seq_courier_id |

**users 核心字段：**
`user_id, username, password, phone, email, gender, birthday, avatar_url, points, member_level, status, create_time`

**admins 核心字段：**
`admin_id, username, password, real_name, role, phone, status, last_login_time, create_time`

role 枚举：`super_admin / store_manager / product_manager / finance / customer_service / warehouse / cashier`

#### 商品相关（4张）

| 表名 | 说明 |
|------|------|
| products | 商品主表 |
| categories | 商品分类（支持树形） |
| brands | 品牌 |
| product_skus | 商品规格（SKU） |

**products 核心字段：**
`product_id, product_name, category_id, brand_id, price, original_price, stock, warning_stock, cover_image, description, barcode, status, sales_count, rating, create_time`

**categories 核心字段：**
`category_id, category_name, parent_id, sort_order, status`

**product_skus 核心字段：**
`sku_id, product_id, sku_name, sku_value, price, stock, barcode`

#### 购物车/订单（4张）

| 表名 | 说明 |
|------|------|
| cart | 购物车 |
| orders | 订单主表 |
| order_items | 订单明细 |
| address | 收货地址 |

**orders 核心字段：**
`order_id, user_id, total_amount, discount_amount, points_deducted, actual_amount, coupon_id, status, payment_method, remark, address_id, create_time, pay_time, deliver_time, complete_time`

status 枚举：`pending_payment / pending_shipment / shipping / pending_receipt / completed / cancelled`

**order_items 核心字段：**
`item_id, order_id, product_id, sku_id, product_name, price, quantity, subtotal`

#### 库存相关（6张）

| 表名 | 说明 |
|------|------|
| inventory_logs | 库存流水（入/出/盘点/报损） |
| purchase_orders | 采购单 |
| purchase_order_items | 采购单明细 |
| stocktake_tasks | 盘点任务 |
| stocktake_items | 盘点明细（含账面库存/实际库存/差异） |
| damage_records | 报损记录 |

**inventory_logs 核心字段：**
`log_id, product_id, change_type, quantity, before_stock, after_stock, remark, operator_id, create_time`

change_type 枚举：`purchase_in / sale_out / stocktake_adjust / damage / return`

#### 营销相关（4张）

| 表名 | 说明 |
|------|------|
| coupons | 优惠券模板 |
| user_coupons | 用户持有优惠券 |
| seckill_activities | 秒杀活动 |
| seckill_products | 秒杀商品 |

**coupons 核心字段：**
`coupon_id, coupon_name, coupon_type, discount_value, min_order_amount, category_id, total_count, received_count, start_time, end_time, status`

coupon_type 枚举：`full_reduction / discount / category`

**seckill_activities 核心字段：**
`seckill_id, activity_name, start_time, end_time, status, create_time`

status 枚举：`pending / running / paused / ended`

#### 配送相关（2张）

| 表名 | 说明 |
|------|------|
| delivery | 配送记录（关联订单） |
| delivery_tasks | 配送员任务 |

**delivery_tasks 核心字段：**
`task_id, order_id, courier_id, status, pickup_time, complete_time, fail_reason, create_time`

status 枚举：`pending / picked_up / completed / failed`

#### 售后/评价（2张）

| 表名 | 说明 |
|------|------|
| after_sale | 售后申请 |
| reviews | 商品评价 |

**after_sale 核心字段：**
`as_id, order_id, user_id, type, reason, image_url, amount, status, handler_remark, create_time`

type 枚举：`refund_only / return_refund`
status 枚举：`pending / approved / rejected / completed`

**reviews 核心字段：**
`review_id, product_id, order_id, user_id, rating, content, tags, is_anonymous, is_hidden, admin_reply, create_time`

#### 消息/积分/收银（3张）

| 表名 | 说明 |
|------|------|
| messages | 站内消息 |
| points_logs | 积分流水 |
| cashier_shifts | 收银班次 |

**messages 核心字段：**
`msg_id, user_id, title, content, msg_type, is_read, create_time`

msg_type 枚举：`order / promotion / refund / system`

**points_logs 核心字段：**
`log_id, user_id, change_type, amount, balance_after, remark, create_time`

**cashier_shifts 核心字段：**
`shift_id, cashier_id, start_time, end_time, start_cash, end_cash, order_count, cash_total, online_total, status`

#### 其他（3张）

| 表名 | 说明 |
|------|------|
| banners | 首页轮播图 |
| audit_logs | 操作审计日志 |
| supplier | 供应商 |

---

## 5. 统一接口规范

### 响应格式

```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

失败时 code 非 200，前端用 `ElMessage.error(res.msg)` 提示。

常用错误码：
- `400` — 参数错误
- `401` — 未登录 / Token 无效
- `403` — 无权限
- `404` — 资源不存在
- `500` — 服务器错误

### 鉴权方式

请求头携带：`Authorization: Bearer <token>`

三类 Token 存储键名：
- 顾客端：`localStorage.getItem('token')`
- 管理端：`localStorage.getItem('adminToken')`
- 配送员端：`localStorage.getItem('courierToken')`

### 公开接口（无需 Token）

```
POST /auth/login
POST /auth/register
POST /auth/admin/login
POST /auth/courier/login
GET  /products/list
GET  /products/{productId}
GET  /products/recommended
GET  /products/categories/tree
GET  /banners/list
GET  /brands/list
GET  /seckill/list
GET  /reviews/product/{productId}
```

### 分页参数约定

请求：`?pageNum=1&pageSize=10`

响应 data 结构（MyBatis-Plus Page）：
```json
{
  "records": [],
  "total": 100,
  "size": 10,
  "current": 1,
  "pages": 10
}
```

---

## 6. API 接口设计

### 6.1 认证模块 `/auth`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/auth/login` | 用户登录 | 公开 |
| POST | `/auth/register` | 用户注册 | 公开 |
| POST | `/auth/admin/login` | 管理员登录 | 公开 |
| POST | `/auth/courier/login` | 配送员登录 | 公开 |
| GET | `/auth/userinfo` | 获取当前用户信息 | 用户 |
| PUT | `/auth/userinfo` | 修改用户基本信息 | 用户 |
| PUT | `/auth/password` | 修改密码 | 用户 |
| GET | `/auth/admin/info` | 获取管理员信息 | 管理员 |

### 6.2 商品模块 `/products`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/products/list` | 商品列表（分页+筛选+排序） | 公开 |
| GET | `/products/{productId}` | 商品详情 | 公开 |
| GET | `/products/recommended` | 推荐商品 | 公开 |
| GET | `/products/categories/tree` | 分类树 | 公开 |
| GET | `/products/{productId}/skus` | SKU列表 | 公开 |
| GET | `/products/barcode/{barcode}` | 按条码查询（收银台用） | 公开 |
| POST | `/products` | 新增商品 | 管理员 |
| PUT | `/products/{productId}` | 修改商品 | 管理员 |
| DELETE | `/products/{productId}` | 删除商品 | 管理员 |
| PUT | `/products/{productId}/status` | 上架/下架 | 管理员 |

商品列表筛选参数：`keyword, categoryId, brandId, minPrice, maxPrice, sortBy(price/salesCount/rating), sortOrder(asc/desc), pageNum, pageSize`

### 6.3 购物车 `/cart`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/cart` | 获取购物车列表 | 用户 |
| POST | `/cart/add` | 添加商品 | 用户 |
| PUT | `/cart/{cartId}` | 修改数量 | 用户 |
| DELETE | `/cart/{cartId}` | 删除单项 | 用户 |
| DELETE | `/cart/clear` | 清空购物车 | 用户 |

### 6.4 订单 `/orders`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/orders` | 创建订单（含库存扣减/积分/优惠券） | 用户 |
| GET | `/orders/list` | 我的订单列表（status筛选） | 用户 |
| GET | `/orders/{orderId}` | 订单详情 | 用户 |
| PUT | `/orders/{orderId}/cancel` | 取消订单 | 用户 |
| PUT | `/orders/{orderId}/confirm` | 确认收货 | 用户 |
| POST | `/orders/{orderId}/pay` | 模拟支付 | 用户 |
| POST | `/orders/cashier` | 收银台下单 | 管理员(收银员) |
| GET | `/admin/orders` | B端订单列表（多条件筛选） | 管理员 |
| PUT | `/orders/{orderId}/admin-cancel` | 强制取消（含原因） | 管理员 |

### 6.5 收货地址 `/addresses`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/addresses` | 我的地址列表 | 用户 |
| POST | `/addresses` | 新增地址 | 用户 |
| PUT | `/addresses/{addressId}` | 修改地址 | 用户 |
| DELETE | `/addresses/{addressId}` | 删除地址 | 用户 |
| GET | `/addresses/default` | 获取默认地址 | 用户 |

### 6.6 售后 `/after-sales`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/after-sales` | 申请售后 | 用户 |
| GET | `/after-sales/my` | 我的售后列表 | 用户 |
| GET | `/after-sales/admin/list` | B端售后列表（状态筛选） | 管理员 |
| PUT | `/after-sales/admin/{id}/handle` | 处理售后（approve/reject） | 管理员 |

### 6.7 评价 `/reviews`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/reviews` | 提交评价 | 用户 |
| GET | `/reviews/product/{productId}` | 商品评价列表 | 公开 |
| PUT | `/reviews/admin/{id}/hidden` | 隐藏/显示评价 | 管理员 |
| PUT | `/reviews/admin/{id}/reply` | 管理员回复 | 管理员 |

### 6.8 优惠券 `/coupons`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/coupons/available` | 可用优惠券（传orderAmount则过滤门槛） | 用户 |
| GET | `/coupons/my` | 我的优惠券（status: unused/used/expired） | 用户 |
| POST | `/coupons/claim/{couponId}` | 领取优惠券 | 用户 |
| GET | `/admin/coupons` | B端优惠券列表 | 管理员 |
| POST | `/admin/coupons` | 新增优惠券 | 管理员 |
| PUT | `/admin/coupons/{couponId}` | 修改优惠券 | 管理员 |
| PUT | `/admin/coupons/{couponId}/status` | 暂停/恢复 | 管理员 |
| DELETE | `/admin/coupons/{couponId}` | 删除优惠券 | 管理员 |

### 6.9 轮播图 `/banners`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/banners/list` | C端轮播图列表（仅启用） | 公开 |
| GET | `/admin/banners` | B端全部轮播图 | 管理员 |
| POST | `/admin/banners` | 新增轮播图 | 管理员 |
| PUT | `/admin/banners/{id}` | 修改轮播图 | 管理员 |
| PUT | `/admin/banners/{id}/toggle` | 启用/禁用 | 管理员 |
| DELETE | `/admin/banners/{id}` | 删除轮播图 | 管理员 |

### 6.10 品牌 `/brands`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/brands/list` | C端品牌列表 | 公开 |
| GET | `/admin/brands` | B端品牌列表（分页） | 管理员 |
| POST | `/admin/brands` | 新增品牌 | 管理员 |
| PUT | `/admin/brands/{brandId}` | 修改品牌 | 管理员 |
| DELETE | `/admin/brands/{brandId}` | 删除品牌（有商品时拒绝） | 管理员 |

### 6.11 秒杀活动 `/seckill`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/seckill/list` | C端进行中秒杀活动（含商品） | 公开 |
| GET | `/admin/seckill` | B端活动列表（分页+状态筛选） | 管理员 |
| POST | `/admin/seckill` | 创建秒杀活动（含商品列表） | 管理员 |
| PUT | `/admin/seckill/{seckillId}/status` | 开始/暂停/结束 | 管理员 |
| POST | `/admin/seckill/{seckillId}/products` | 添加秒杀商品 | 管理员 |
| DELETE | `/admin/seckill/{seckillId}/products/{id}` | 移除秒杀商品 | 管理员 |

### 6.12 站内消息 `/messages`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/messages` | 我的消息列表（msgType筛选，分页） | 用户 |
| GET | `/messages/unread-count` | 未读消息数 | 用户 |
| PUT | `/messages/{msgId}/read` | 标为已读 | 用户 |
| PUT | `/messages/read-all` | 全部已读 | 用户 |
| POST | `/admin/messages` | 发送站内信给用户 | 管理员 |

### 6.13 积分流水 `/users/points-logs`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/users/points-logs` | 我的积分流水（分页） | 用户 |
| PUT | `/admin/users/{userId}/points` | 手动调整积分 | 管理员 |

### 6.14 收银班次 `/cashier`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/cashier/shift/open` | 开班（传startCash备用金） | 管理员 |
| GET | `/cashier/shift/current` | 当前班次状态 | 管理员 |
| POST | `/cashier/shift/close` | 交班（传endCash清点金额） | 管理员 |
| GET | `/cashier/shift/history` | 历史班次记录（分页） | 管理员 |
| GET | `/cashier/products/search` | 商品搜索（keyword/barcode） | 管理员 |

### 6.15 配送员端 `/courier`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/courier/info` | 个人信息+今日已送单数+累计单数 | 配送员 |
| PUT | `/courier/password` | 修改密码 | 配送员 |
| GET | `/courier/tasks` | 待配送任务列表（status=pending） | 配送员 |
| PUT | `/courier/tasks/{taskId}/pickup` | 标记已取件 | 配送员 |
| PUT | `/courier/tasks/{taskId}/complete` | 标记已送达 | 配送员 |
| PUT | `/courier/tasks/{taskId}/fail` | 标记配送失败（含原因） | 配送员 |
| GET | `/courier/tasks/history` | 历史任务记录 | 配送员 |
| GET | `/admin/couriers` | 配送员列表 | 管理员 |
| POST | `/admin/couriers` | 新增配送员 | 管理员 |
| PUT | `/admin/couriers/{courierId}` | 修改配送员信息 | 管理员 |
| PUT | `/admin/deliveries/{deliveryId}/assign` | 分配配送员 | 管理员 |

### 6.16 库存盘点 `/admin/stocktake`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/admin/stocktake` | 创建盘点任务（scope: all/category） | 管理员 |
| GET | `/admin/stocktake` | 盘点任务列表（分页+状态筛选） | 管理员 |
| GET | `/admin/stocktake/{taskId}` | 盘点详情（含每个商品账面/实际库存） | 管理员 |
| PUT | `/admin/stocktake/{taskId}/items` | 批量录入实际数量 | 管理员 |
| PUT | `/admin/stocktake/{taskId}/submit` | 提交盘点（差异写库存流水） | 管理员 |

### 6.17 报损 & 数据看板（AdminController）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/admin/damage-records` | 新增报损（自动扣库存+写流水） | 管理员 |
| GET | `/admin/damage-records` | 报损记录列表（分页+时间筛选） | 管理员 |
| GET | `/admin/statistics/sales-trend` | 销售趋势（type: day/week/month） | 管理员 |
| GET | `/admin/statistics/product-rank` | 商品销售排行（limit, sortBy） | 管理员 |
| GET | `/admin/statistics/category-sales` | 分类销售占比 | 管理员 |
| GET | `/admin/statistics/hourly-sales` | 24小时时段销售分析 | 管理员 |
| GET | `/admin/statistics/user-trend` | 新增用户趋势 | 管理员 |
| GET | `/admin/statistics/coupon-stats` | 优惠券核销统计 | 管理员 |

---

## 7. 项目目录结构

```
SupermarketSystem/
├── backend/
│   ├── pom.xml
│   ├── API文档.md
│   └── src/main/java/com/supermarket/
│       ├── common/
│       │   ├── Result.java
│       │   ├── BusinessException.java
│       │   └── GlobalExceptionHandler.java
│       ├── config/
│       │   ├── WebConfig.java          # 拦截器注册 + 公开路径
│       │   └── MybatisPlusConfig.java  # 分页插件
│       ├── controller/                 # 17个 Controller
│       ├── dto/                        # 请求体 DTO
│       ├── entity/                     # 28个实体类
│       ├── interceptor/
│       │   └── JwtInterceptor.java
│       ├── mapper/                     # 28个 Mapper
│       └── service/                    # 13个 Service
│
├── frontend/
│   ├── package.json
│   ├── vite.config.js                  # 代理配置：/api → localhost:8080
│   └── src/
│       ├── api/index.js                # 所有接口统一封装
│       ├── router/index.js             # 路由配置
│       ├── stores/user.js              # Pinia 用户状态
│       ├── utils/request.js            # Axios 实例 + 拦截器
│       └── views/
│           ├── Login.vue
│           ├── Layout.vue
│           ├── user/                   # 顾客端
│           ├── admin/                  # 管理后台
│           ├── cashier/                # 收银端
│           └── courier/               # 配送员端
│
├── database/
│   ├── all_tables.sql                  # v3.0 完整版（唯一需执行的脚本）
│   └── run_sql.py                      # Python执行脚本（解决编码问题）
│
├── README.md
├── 系统设计文档.md
├── 项目总结.md
└── 前端分工文档.md
```

---

## 8. 前端开发指引

### API 调用约定

所有接口统一通过 `frontend/src/api/index.js` 调用，禁止在组件中直接写 axios。

### Token 键名对照

| 端 | Token 键名 | 路由前缀 |
|----|-----------|----------|
| 顾客端 | `token` | `/` |
| 管理后台/收银端/仓储端/看板端 | `adminToken` | `/admin`, `/cashier` |
| 配送员端 | `courierToken` | `/courier` |

### 统一错误处理

```js
if (res.code !== 200) {
  ElMessage.error(res.msg)
  return Promise.reject(res)
}
```

### 后端接口地址

后端运行在 `http://localhost:8080`，`vite.config.js` 已配置代理，前端直接用相对路径调用。

详见 [`前端分工文档.md`](./前端分工文档.md) 了解各成员具体页面与接口分配。
