# 超市管理系统 · 开发进度分析报告
> 生成时间：2026-03-23
> 参考文档：超市管理系统_完整功能清单v3.0.docx

---

## 零、项目结构说明

本系统共有 **两套代码**，真正在用的是前后端分离版本：

| 路径 | 技术栈 | 说明 |
|------|--------|------|
| `D:\桌面\SupermarketSystem` | Servlet + JSP + Oracle | 旧版，已废弃，无需继续开发 |
| `C:\Users\zz\backend` | **Spring Boot 3.2 + MyBatis Plus + JWT + Oracle** | ✅ 当前主项目后端 |
| `C:\Users\zz\frontend` | **Vue 3 + Vite + Element Plus + Pinia** | ✅ 当前主项目前端 |

---

## 一、技术栈总览

### 后端（`C:\Users\zz\backend`）
- **框架**：Spring Boot 3.2.0 + MyBatis Plus 3.5.5
- **数据库**：Oracle XE（localhost:1521:XE）
- **认证**：JWT（jjwt 0.12.3，Token有效期24小时）
- **Java版本**：17
- **端口**：8080
- **其他**：Lombok、Apache POI（Excel导出）、Hibernate Validator

### 前端（`C:\Users\zz\frontend`）
- **框架**：Vue 3.4.0 + Composition API
- **构建**：Vite 5.0.8
- **UI库**：Element Plus 2.4.4
- **路由**：Vue Router 4.2.5
- **状态管理**：Pinia 2.1.7
- **请求**：Axios 1.6.2（代理到 localhost:8080）
- **端口**：3000

---

## 二、数据库建表完成度分析

### 📊 总体情况
| 项目 | 数量 |
|------|------|
| 需求文档要求总表数 | **30 张** |
| 当前 Entity 类数量 | **14 个**（对应约14张表） |
| 缺少表数 | **约 16 张** |
| 完成率 | **约 47%** |

> 注：后端使用 JPA（ddl-auto: none），表需手动建立，需核对数据库中实际建表情况。

---

### ✅ 已有 Entity（后端已定义，表可能已建）

| # | Entity类 | 对应表 | 存在的问题 |
|---|----------|--------|-----------|
| 1 | `User` | `USERS` | ⚠️ 缺少字段：nickname、avatar、gender、birthday、email、member_level、points、ban_reason；role字段仅admin/user，未支持6种后台角色 |
| 2 | `Product` | `PRODUCTS` | ⚠️ 缺少字段：barcode(条码)、image_url(图片)、brand_id、original_price(原价)、cost_price(成本价)、stock_warning(预警阈值)、is_recommend(首页推荐)、is_deleted(逻辑删除) |
| 3 | `Category` | `CATEGORIES` | ⚠️ 缺少字段：parent_id（支持二级分类）、sort_order（排序）、icon |
| 4 | `Order` | `ORDERS` | ⚠️ 缺少字段：order_no(业务订单号)、address_id(收货地址)、freight(运费)、discount_amount(优惠金额)、actual_amount(实付)、points_used(积分抵扣)、coupon_id、remark、source(线上/收银)、delivery_time |
| 5 | `OrderItem` | `ORDER_ITEMS` | ⚠️ 缺少字段：sku_id、product_name(商品名快照)、spec_name(规格快照) |
| 6 | `Cart` | `CART` | ⚠️ 缺少字段：sku_id、is_checked（是否勾选结算） |
| 7 | `Address` | `ADDRESSES` | 基本完整，需确认province/city/district三级字段是否齐全 |
| 8 | `Delivery` | `DELIVERIES` | ⚠️ 缺少字段：courier_id(配送员外键)、pickup_time(取件时间)、fail_reason(失败原因) |
| 9 | `InventoryLog` | `INVENTORY_LOGS` | ⚠️ 缺少字段：before_stock(变动前)、after_stock(变动后)、ref_no(关联单号) |
| 10 | `Supplier` | `SUPPLIERS` | 需确认字段是否包含：联系人、手机、邮箱、地址、结算账期 |
| 11 | `Promotion` | `PROMOTIONS` | ⚠️ 结构过于简单，需拆分为秒杀表和满减表 |
| 12 | `PurchaseOrder` | `PURCHASE_ORDERS` | 需确认是否有采购明细表 |
| 13 | `Payment` | `PAYMENTS` | 需确认字段完整性 |
| 14 | `AuditLog` | `AUDIT_LOGS` | 需确认是否有操作前后快照字段 |

---

### ❌ 完全缺失的 Entity 和表（需新建，共约16张）

#### 🔴 P0 优先级（核心功能，必须先建）

| # | 表名 | 描述 | 关键字段 |
|---|------|------|---------|
| 1 | `ADMINS` | 后台管理员表（与普通用户分离） | admin_id, username, password, real_name, role(6种角色), phone, status, last_login_time |
| 2 | `PRODUCT_SKUS` | 商品SKU规格表 | sku_id, product_id, spec_name, spec_value, price, stock, barcode |
| 3 | `COUPONS` | 优惠券模板表 | coupon_id, name, type(满减/折扣/品类), min_amount, discount, total_count, per_limit, start_time, end_time, status, category_id |
| 4 | `USER_COUPONS` | 用户领取的优惠券 | id, user_id, coupon_id, status(未使用/已使用/已过期), receive_time, use_time, order_id |
| 5 | `AFTER_SALES` | 售后/退款申请表 | after_sale_id, order_id, user_id, type(仅退款/退货退款), reason, description, images, status(待处理/已同意/已拒绝), refund_amount, handle_time |
| 6 | `REVIEWS` | 商品评价表 | review_id, order_id, product_id, user_id, rating(1-5), content, images, tags, is_anonymous, is_hidden, admin_reply, create_time |
| 7 | `COURIERS` | 配送员表 | courier_id, name, phone, password, status, is_disabled, today_count, total_count, create_time |

#### 🟡 P1 优先级（重要功能，第二批建）

| # | 表名 | 描述 | 关键字段 |
|---|------|------|---------|
| 8 | `BRANDS` | 品牌表 | brand_id, name, logo_url, description, sort_order |
| 9 | `MESSAGES` | 站内消息通知表 | msg_id, user_id, title, content, type(订单/促销/系统/退款), is_read, create_time |
| 10 | `POINTS_LOGS` | 积分流水表 | log_id, user_id, change_amount, balance_after, type(购物获得/消费抵扣/手动调整/退款回滚), remark, create_time |
| 11 | `BANNERS` | 首页Banner轮播图 | banner_id, image_url, link_type, link_target, sort_order, start_time, end_time, is_active |
| 12 | `SECKILL_ACTIVITIES` | 秒杀活动场次表 | seckill_id, name, start_time, end_time, status(未开始/进行中/已暂停/已结束) |
| 13 | `SECKILL_PRODUCTS` | 秒杀商品表 | id, seckill_id, product_id, seckill_price, seckill_stock, sold_count |
| 14 | `PURCHASE_ORDER_ITEMS` | 采购单明细表 | item_id, po_id, product_id, quantity, actual_quantity, unit_price |
| 15 | `STOCKTAKE_TASKS` | 库存盘点任务表 | task_id, scope(全部/分类), category_id, status(待盘点/盘点中/已完成), creator_id, create_time |
| 16 | `STOCKTAKE_ITEMS` | 盘点明细表 | id, task_id, product_id, book_stock, actual_stock, difference, reason |

#### 🟢 P2 优先级（锦上添花）

| # | 表名 | 描述 |
|---|------|------|
| — | `PRODUCT_FAVORITES` | 商品收藏表 |
| — | `CASHIER_SHIFTS` | 收银班次表 |
| — | `DAMAGE_RECORDS` | 报损记录表 |

---

## 三、前端页面完成度分析

### 已有的 Vue 页面（`frontend/src/views/`）

#### 管理后台（admin/）
| 页面 | 完成情况 |
|------|---------|
| Dashboard（看板） | 🟡 有，需对接数据 |
| Products（商品管理） | 🟡 有，缺少SKU、品牌、图片上传 |
| Orders（订单管理） | 🟡 有，缺少分配配送员、售后等 |
| Users（用户管理） | 🟡 有，缺少封禁、积分调整 |
| Inventory（库存） | 🟡 有，缺少采购单、盘点、报损 |
| Finance（财务） | 🟡 有，功能范围待确认 |
| Categories（分类） | 🟡 有，缺少二级树形 |
| Promotions（促销） | 🟡 有，缺少秒杀、优惠券完整体系 |
| Deliveries（配送） | 🟡 有，缺少配送员账号管理 |

#### 用户端（user/）
| 页面 | 完成情况 |
|------|---------|
| Home（首页） | 🟡 有，缺少Banner轮播、活动入口 |
| Products（商品列表） | 🟡 有，缺少分类筛选、搜索联想、SKU选择 |
| Cart（购物车） | 🟡 有，缺少勾选结算、失效商品处理 |
| Checkout（结算） | 🟡 有，缺少优惠券、积分抵扣、配送时间选择 |
| Orders（订单列表） | 🟡 有，缺少状态Tab切换 |
| OrderDetail（订单详情） | 🟡 有，缺少配送进度、售后入口 |
| Profile（个人信息） | 🟡 有，缺少头像上传、会员等级展示 |
| Address（收货地址） | 🟡 有，需确认三级联动 |

#### 完全缺失的页面
| 缺失页面 | 所属端口 | 优先级 |
|---------|---------|-------|
| 收银端（班次+收银台） | 收银端 | P0 |
| 配送员端（任务列表+更新状态） | 配送员端 | P0 |
| 仓储进货端（采购+盘点） | 仓储端 | P1 |
| 优惠券中心（领取+我的优惠券） | 用户端 | P1 |
| 售后申请+进度查看 | 用户端 | P1 |
| 商品评价页面 | 用户端 | P1 |
| 消息通知中心 | 用户端 | P1 |
| 积分流水记录 | 用户端 | P1 |
| 数据大屏（ECharts看板） | 数据看板端 | P1 |

---

## 四、后端 Controller/API 完成度

### 已有 Controller
| Controller | 覆盖功能 | 缺失功能 |
|-----------|---------|---------|
| `AuthController` | 登录、注册 | 配送员登录、管理员多角色登录 |
| `ProductController` | 商品增删改查 | SKU管理、图片上传、品牌/供应商关联、逻辑删除 |
| `OrderController` | 下单、订单列表 | 分配配送员、取消订单、确认收货、售后 |
| `CartController` | 购物车操作 | 勾选结算、失效商品处理 |
| `AddressController` | 地址增删改查 | 三级地区数据接口 |
| `AdminController` | 部分管理功能 | 6种角色权限控制、审计日志 |

### 完全缺失的 Controller（需新建）
- `CouponController` — 优惠券模板管理、用户领取、下单使用
- `ReviewController` — 评价提交、管理后台审核
- `AfterSaleController` — 售后申请、退款审核处理
- `CourierController` — 配送员账号管理、任务管理
- `DeliveryTaskController` — 配送任务列表、状态更新
- `MessageController` — 站内消息查询、标记已读
- `PointsController` — 积分流水查询、手动调整
- `BannerController` — 首页Banner管理
- `BrandController` — 品牌管理
- `SeckillController` — 秒杀活动管理
- `PurchaseOrderController` — 采购单流程
- `StocktakeController` — 盘点任务管理
- `DamageController` — 报损登记
- `CashierController` — 收银台+班次管理
- `DashboardController` — 数据看板统计接口

---

## 五、接下来的工作计划

### 第一阶段：数据库补全（建议1-2天）

**1.1 修复现有Entity字段缺失（修改已有表）**
- [ ] `users` 表：加 nickname, avatar, gender, birthday, email, member_level, points, ban_reason
- [ ] `products` 表：加 barcode, image_url, brand_id, original_price, cost_price, stock_warning, is_recommend, is_deleted
- [ ] `categories` 表：加 parent_id, sort_order
- [ ] `orders` 表：加 order_no, address_id, freight, discount_amount, actual_amount, points_used, coupon_id, remark, source, delivery_time
- [ ] `order_items` 表：加 sku_id, product_name(快照), spec_name(快照)
- [ ] `cart` 表：加 sku_id, is_checked
- [ ] `deliveries` 表：加 courier_id, pickup_time, fail_reason
- [ ] `inventory_logs` 表：加 before_stock, after_stock, ref_no

**1.2 新建缺失表（Oracle SQL）**
- [ ] `ADMINS`（管理员表，含6种角色）
- [ ] `PRODUCT_SKUS`（SKU规格）
- [ ] `COUPONS` + `USER_COUPONS`（优惠券体系）
- [ ] `AFTER_SALES`（售后退款）
- [ ] `REVIEWS`（评价）
- [ ] `COURIERS`（配送员）
- [ ] `BRANDS`（品牌）
- [ ] `MESSAGES`（站内消息）
- [ ] `POINTS_LOGS`（积分流水）
- [ ] `BANNERS`（首页Banner）
- [ ] `SECKILL_ACTIVITIES` + `SECKILL_PRODUCTS`（秒杀）
- [ ] `PURCHASE_ORDER_ITEMS`（采购明细）
- [ ] `STOCKTAKE_TASKS` + `STOCKTAKE_ITEMS`（盘点）

**1.3 同步更新 Spring Boot Entity 类和 Mapper**
- [ ] 对应每张新表/修改表，更新Java Entity类字段
- [ ] 新建 Mapper 接口

---

### 第二阶段：后端 API 开发（建议3-5天）

**P0 核心接口**
- [ ] 完善用户注册（手机号+验证码mock）、个人信息修改、头像上传
- [ ] 管理员多角色登录 + JWT权限拦截（6种角色）
- [ ] 商品SKU查询、规格选择
- [ ] 订单完整状态流转（待付款→模拟支付→待发货→配送中→待收货→已完成）
- [ ] 库存扣减（下单时）+ 库存回滚（取消/售后时）
- [ ] 配送员账号创建 + 分配配送员 + 任务状态更新

**P1 重要接口**
- [ ] 优惠券：创建/领取/下单使用/核销
- [ ] 积分：下单获得/抵扣/手动调整/流水查询
- [ ] 售后：申请/审核/退款/库存回滚
- [ ] 评价：提交/查看/后台隐藏/商家回复
- [ ] 站内消息：发送/查询/标记已读
- [ ] Banner：增删改查/启用禁用
- [ ] 秒杀：活动管理/秒杀商品管理/秒杀下单
- [ ] 采购单：创建/审批/到货入库
- [ ] 盘点：创建任务/录入数量/提交报告
- [ ] 收银班次：开班/交班/收银下单
- [ ] 数据看板统计接口：销售额/订单数/趋势/排行/分类占比

---

### 第三阶段：前端 Vue 页面开发（建议3-5天）

**P0 优先补全**
- [ ] 商品详情页 SKU规格选择，价格/库存实时切换
- [ ] 购物车勾选结算逻辑
- [ ] 结算页：优惠券选择、积分抵扣、配送时间
- [ ] 订单状态Tab切换（待付款/配送中/已完成等）
- [ ] 收银端页面（班次管理 + 收银台）
- [ ] 配送员端H5页面（任务列表+状态更新）

**P1 重要补全**
- [ ] 用户端：优惠券中心、售后申请、评价页、消息通知、积分流水
- [ ] 管理后台：品牌管理、供应商管理、Banner管理、秒杀管理、优惠券管理、审计日志
- [ ] 仓储端页面（采购 + 盘点 + 报损）
- [ ] 数据看板大屏（引入 ECharts，折线图/柱状图/饼图）

---

### 第四阶段：联调测试（建议1-2天）
- [ ] 完整下单→支付→配送→收货→评价全链路测试
- [ ] 库存扣减并发测试
- [ ] 各角色权限隔离测试
- [ ] 优惠券/积分抵扣金额准确性验证
- [ ] 数据看板统计数据验证

---

## 六、建议开发顺序

```
第1步 → 补全数据库（修改现有表 + 新建缺失表），同步更新Entity
第2步 → 完善用户注册/登录 + JWT权限体系（6种角色）
第3步 → 订单完整流程后端接口（核心主链路）
第4步 → 前端订单流程页面打通（购物→下单→模拟支付）
第5步 → 优惠券+积分体系（后端+前端）
第6步 → 售后/退款功能
第7步 → 配送员端（后端+前端H5）
第8步 → 收银端（后端+前端）
第9步 → 仓储端（采购+盘点）
第10步 → 数据看板（ECharts图表）
第11步 → 全面联调测试
```

---

*本报告由 Claude Code 自动分析生成 · 2026-03-23*
